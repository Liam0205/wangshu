# Wangshu llmdoc 文档地图

> 项目状态:**P1(crescent 解释器)+ P2(bridge 分层桥,PB0-PB7 + 后续优化轮 #1-#4)+ P3(gibbous/wasm 编译层,PW0-PW10)全卷已收口;P4(method JIT)多 PJ 已完成(PJ0-PJ11 + PJ10 native emit,2026-07-01),P5 仍为规划**——P1 全卷(M0-M14 + 收尾轮 + 测试加固轮 + 完整性补全轮 + 长稳承诺轮 + 外部审查修复轮 + 官方测试套与性能轮)与 P2 全卷(2026-06-13 单会话 PB0-PB7 冲刺 + 同会话 P2 后续优化轮 #1-#4:bridge 包骨架 + 回边/入口采样 + IC 反馈聚合 + F1-F7 可编译性检查 + TierState 状态机 + Logger + mock P3 + e2e 验收 + stdlib 白名单 + 阈值校准占位 + sync.Pool (C) 双表混合 + megamorphic 主动识别)交付。验收:simple 9.0x / arith 7.0x / loop 2.45x over gopher-lua;realworld 五项中四项反超(fib 1.31x / binary-trees 1.09x / spectral-norm 1.43x / nbody 1.08x;fannkuch 0.82x 为剩余短板);70 种子 + 随机脚本差分测试官方 5.1.5 逐字节一致;P2 通过四档验收(byte-equal、profile 累积、升层日志、多 State `-race`)。**P3 PW0-PW10 全卷已收口**(PW0 spike 检查过线 wazero call boundary 36.7ns<150ns → PW1 包骨架 + arena 收养 wazero memory → PW2 直线 opcode + crescent↔gibbous trampoline 端到端 + VS0 值栈 arena 迁移 → PW3 算术/比较 opcode(f64 快路径 + slow-path 助手)→ PW4 控制流 + relooper 结构化生成(循环/分支/嵌套逐字节一致)+ FORPREP/FORLOOP → PW4b TFORLOOP 泛型 for(经 h_tforloop 调迭代器 + base 刷新)→ PW5 表 IC opcode(GETGLOBAL/SETGLOBAL/GETTABLE/SETTABLE/SELF inline IC 快照固化跳哈希 + NEWTABLE/SETLIST 走助手,失效逐字节降级、零 deopt)→ PW6 CALL/TAILCALL 跨层三向互调(base 刷新解 growStack 段重定位 UAF + 错误 status 链穿越冒泡)→ PW7 CLOSURE/CLOSE(VARARG 白名单拒)→ PW8 线程级 tier 规则(协程不升层,onMain bool 升层门禁)→ PW9 端到端总验收(正确性轴 V1-V13 三方逐字节 + V17 四 build + V18 -race + 性能轴 V14 loop 核 2.58x≥2x 全过;V15 geomean 0.79x 未过,跨层调用税拆 PW10);全 38 opcode 除 VARARG(F1 排除)外全部可翻译。**PW10 消除 gibbous→gibbous 跨层调用税:R1/R2/R3/R3.5 + 零跨界 ①/基建-a/基建-b/③a/③b/④-i 骨架/顶层升层 callOnStack 全过线**(R1 memadapter env holder 导出共享 funcref 表 TableSlots=8192,各 gibbous module active element 段自注册 `run` / R2 完整 CallInfo→linear-memory 迁移 = 长期延后的 VS0-e,4 word/帧,R2a→R2b-1~4 收口/只写影子/翻转/退役 / R3 `call_indirect` 直接分派消 `code.Run` 重入,3 路 i64 sentinel + `ciTransferRef` 中转字 + `h_callerr` 弹孤立帧 / R3c-fix 出错点就地标注使错误逐字节等纯解释器 / R3.5 host helper `WithFunc`→`WithGoFunction` 消反射装箱 / 零跨界 ① top mirror 字 + 基建-a closure slot 缓存 + ③a savedTop + ③b emitReturn 守卫快路径 / 基建-b proto cache 段 + ④-i emitCall 守卫骨架 + fastCallHits mirror 字 / callOnStack 顶层升层 cl 直接走 enterGibbous + TopLevelUplift 探针);**本机 Xeon 6982P 2s×3 count 实测基线(2026-06-16)**:**loop 2.95x(+10% over R3.5 2.67x,③ RETURN 拆帧真实收益)/ table 0.88x / call 0.52x / mixed 0.99x**;**call 0.52x 是 bench kernel 结构性架构边界**——profile `/tmp/call.prof` 实证四 kernel body 含 ReasonUnknownCall(F2-b 静态分析不能确定被调函数不 yield)→ body 不可升 → 顶层升层 + ④ emitCall fast body 均对 bench kernel 无显著效果;**④-ii fast body 未交付**(预估上限 0.57x 仍 <1x,实现复杂度 ~200 行 wasm 字节级 codegen,UAF 高,ROI/UAF 不利留 followup,emit 原语 i64.add/i64.or 已保留供未来 ④-ii);**PW10 收口为「已完成子里程碑 + 架构边界文档化」**,旧文档所述「四核全翻面」+「剩 R4/R5 待实现」失实须替换;**VS0-e 全量收口(2026-06-16)**——task #6 描述三项里两项早隐性交付(协程栈 VS0-c / CallInfo word 位打包 PW10 R2),本会话补齐 varargs 进栈下区子步 ①~④(`c22798b`/`4e50687`/`966318c`/`ed95020` + 反思 `b9a2c54`),官方 Lua 5.1 真栈布局 `[func | vararg | R(0)..]` 完成,退役 `ci.varargs` Go slice + `th.ciVarargs` 影子 + setVarargs/clearVarargs/varargsAt 三 API,统一栈模型 + 简化 GC 路径达成;**P4 设计文档集已扩展(2026-06-24)**——单文件 360 行 → 子目录 10 文件 ~8200 行(00-08 + implementation-progress,P3 同档详细深度),含 PJ0-PJ11 里程碑预设 + V1-V22 验收口径 + 34 项跨文档回填请求登记(方案 A:P4 自管投机生命周期 / P2 三态枚举不变;原 RJ-8/9/10 撤回);P5 仍是单文件架构决策深度**。`docs/design/` 共约 44 篇约 3.8 万行设计文档仍是规范源(P1 全卷 00-12 可实现深度、**P2 全卷 00-06 + implementation-progress 可实现深度(PB0-PB7 + 后续优化轮 #1-#4 全过线)**、**P3 全卷 00-08 + implementation-progress 可实现深度(子目录 10 文件 ~8800 行,含全 opcode 翻译表 + WAT 伪码 + V1-V18 验收口径;PW0-PW9 + PW4b 全过线)**、**P4 全卷 00-08 + implementation-progress 详细设计深度(子目录 10 文件 ~8200 行,2026-06-24 扩展,含 PJ0-PJ11 + V1-V22 + 34 项回填请求,方案 A:P4 自管投机生命周期/P2 状态机不变;原 RJ-8/9/10 撤回)**、P5 架构决策);P1 实现现状与 P3 迁移留口见 `docs/design/p1-interpreter/implementation-progress.md` 对账表;P2 实施现状对账表见 `docs/design/p2-bridge/implementation-progress.md`;P3 实施现状对账表见 `docs/design/p3-wasm-tier/implementation-progress.md`;**P4 实施现状(立项前)对账表见 `docs/design/p4-method-jit/implementation-progress.md`**(含 34 项 RJ 回填请求,方案 A)。本文档库是源文档之上的**知识压缩层**,记录意图与路由。
> 启动阅读顺序请看 [[startup]](本文件不重复有序启动清单)。
> **术语:`P2+` = P2 后续优化轮**(P2 主体 PB0-PB7 交付之后的持续优化项),**不是 P3 阶段**——本轮 2026-06-13 已把 P2 后续优化轮 #1-#4 全部融入 P2 主交付。见 design `p2-bridge/` 各篇与 `implementation-progress.md` §0 头注。
> **术语:`PW`(P-Wasm)= P3 实现里程碑编号**(对应 P1 的 M、P2 的 PB);PW0 = wazero call boundary spike 检查,PW1-PW9 = 翻译器到端到端验收。见 `docs/design/p3-wasm-tier/00-overview.md` §4。

## 类别用途

- **`must/`** —— 每次任务都应先读的微型启动文档。只放跨任务、稳定、几乎每次都用得上的知识。
- **`overview/`** —— 项目/大特性的身份、边界与角色。
- **`architecture/`** —— 检索地图、所有权边界、流程与不变式。
- **`guides/`** —— 一篇一个工作流。
- **`reference/`** —— 稳定查阅事实:契约、schema、约定、术语。
- **`memory/`** —— 历史过程记忆。`memory/reflections/` 归 reflector 所有;`memory/decisions/` 与 `memory/doc-gaps.md` 归 recorder 所有。

## 现有文档与路由提示

### must/
- [[design-premises]] — **最重要的 MUST**。四组不可妥协前提:① 列内核负载形状(两个校准测量:LuaJIT 仅比 luajc 快 6%;生产端到端被稀释到 ±5-7% 噪声)② Go runtime 四项税 → 边界几十~百 ns 固定成本 ③ 五条贯穿原则 ④ 第一天 NaN-boxing 值表示承诺。**判断任何提案是否合理前先读这篇。**

### overview/
- [[project-overview]] — 项目身份与边界:望舒/Lua=月亮意象、纯 Go 嵌入式 Lua VM 定位、三层目标(Lua 5.1 核心)、三项非目标(§6)、首个目标宿主(多运行时规则引擎)、当前状态。**想知道「这是什么、不做什么」看这篇。**

### architecture/
- [[evolution-roadmap]] — 分层 VM 五阶段流水线 P1→P5(人力/倍率/验收/前置 spike)、月相 tier 命名映射。**问演进路线、阶段门槛、wazero<150ns spike、crescent/gibbous/fullmoon 看这篇。** 注意流水线图倍率与正文验收门槛不在同一坐标系。
- [[value-representation]] — 值表示与内存模型:NaN-boxing vs Go tagged struct 决策、自管 arena、自写 mark-sweep GC、同一块内存使编译层成增量。**问值/内存/GC/为什么这样选看这篇。**

### reference/
- [[embedding-contract]] — 宿主嵌入契约:`Compile→Program`、`Program.Call(state, arena, args)`(收尾轮已完成,差异标注见该篇)、arena ABI(类型化扁平列 + 字符串区 + presence bitmap,零拷贝读)、per-item 简易 API 子集已完成(含 Table 读写全闭环 + ForEach + globals baseline 状态隔离 + `CallInto` 零分配边界路径 + issue #13 parity-friendly 快路径 typed-array table 族 / `GlobalsSlot` + issue #10 批量 array table 构造 `NewArrayTable`/`Preallocate`)、State 生命周期 admin API(issue #9/#11:`Options.{Initial,Max}ArenaBytes` + `ArenaCapKB`/`GCCountKB` 观测 + `Collect`/`MaybeCollectNow` GC 节奏显式驱动 + `SetHostTriggeredCollect` experimental)、drop-in 定位。字段级 spec 在 `docs/design/p1-interpreter/11-embedding-arena-abi.md`。**问宿主怎么嵌入、API 形状、边界成本/CallInto、GC 节奏/arena 容量管理看这篇。**
- [[glossary]] — 术语表 + prior art 借鉴点。**遇到 NaN-boxing/arena/tier/月相/deopt/列内核等术语,或问参照项目看这篇。**

### guides/
- [[multi-doc-drafting]] — 多文档并行起草工作流:回填请求节协议、单点收口、验收口径收口点指定、子代理失败恢复纪律、收尾主动盘点不确定决策、向用户提问自包含契约。**要一次起草多篇互引文档、或大型设计任务收尾时看这篇。**
- [[perf-optimization-workflow]] — 性能优化工作流:profile 先行(预判清单会被推翻)、每项独立 benchmark 验证 + 单域提交、可疑优化 benchmark 否决门 + 快 revert、归因诚实、**跨机器/跨参数基线对照**(同 commit 同硬件同参数复测,memory/reflection 写 perf 数字必标硬件/参数/日期)、池化/复用类优化配套清单。**做性能优化前看这篇。**
- [[public-api-incremental-delivery]] — 公共 API 增量交付工作流(9 条纪律):设计承诺源三角验证、GCRef 接根(指向 [[embedding-contract]] 不变式条款)、单域物理隔离手法、错误消息稳定语义、行为变更显式标注、范围扩张顺手收口、对位测试断言先 grep oracle、internal 接口签名避免反向依赖标准库、**对称面检查**(写入/读出闭环)。**接公共面 issue / 扩 ABI 表面 / drop-in 对位补面 / 给 Program 加字段时看这篇。**
- [[design-claims-vs-codebase-physics]] — 设计稿主张须对本码库 physics 重新验证(6 维判据,四空间内部 + 一时间 + 一空间外部):设计稿抽象记号只表语义不带物理不变式,热路径主张实现前逐条重核——① 边界成本预算(~143ns/跨层,`(call $x)` 能否塌成 inline)② arena 段重定位(跨层固定 token/视图会不会被 grow 搬走、谁刷新)③ 成本归类(架构成本 vs 实现浪费,援引前提判否优化前先分类)④ GC 根可达性(复用栈/共享 arena 返回值的根)**⑤ 时间维度**(设计稿/task 描述/stub sentinel 注释承诺/外部依赖现状在前序事实变更后失效,需显式审计;含 PW7 难点过期 / VS0-e 调研先于实现 / P4 doc-review 外部依赖现状 / PR #28 sentinel 注释承诺与检查翻 true 解耦 / issue #67 promotion 烤死快照 vs 运行期跨 Run 重建(身份 guard 跨 Run 落空,换 key/shape 内容 guard)五实例)**⑥ 空间维度**(同一时刻跨 OS / shell / runtime / runner 物理环境差异:bash 3.2 vs 4+ cheatsheet / actions/cache 对 symlink 的 tar 语义 / homebrew 包政策更迭;PR #28 三平台矩阵 CI 三连实例,跨过提升阈值)。横跨性能(死优化)+ 正确性(UAF + 静默 sentinel)+ 跨部署环境(静默 build break)。**照抄设计稿热路径上的 `(call $x)`/固定 token/成本主张前、检查翻 true 同 commit、接延后 ≥6 个月的 task 前、大文档发布前、CI 矩阵扩平台前、写 shell 脚本要跨 OS 跑前看这篇。**
- [[prove-the-path-under-test]] — 证明在测的路径(绿色 ≠ 在测你以为在测的):测试通过 / 性能数字不动 / 新机制就位**单独任何一个都不证明在测路径被执行**——反模式三档(空测 / 不公平基准 / 静默替身 / 覆盖度自欺)+ 反向侧解药四招(毒化哨兵 helper / 白盒命中计数器 / 非空载体先证路径再读数 / 错误路径用例)+ 正向侧解药三招(§4 覆盖度先 grep 既有 oracle 如 luasuite/官方 5.1 套再决定是否补语料 / **§5 fuzz 探索空间维度动了就重探**:接受面 / 硬件 / OS / 运行参数都是 fuzz 探索空间的维度,任一维动一格就等于新一轮独立探索,变化后立即跑一轮 60~120s fuzz smoke 是廉价高产的默认动作;含 P4 opSupported 扩面三 seed 抓 inline 快路径 bug + M5 Pro 抓 amd64 长期未抓到的 fatal + LEN/MOD/POW 进 opSupported 90s 抓 PerOpCode 回放乱序老 bug 三实例 / **§6 多档机制叠加崩点的 bypass 探针根因 isolate**:N 档机制叠加一样的 SIGSEGV/SIGILL 形式时写 minimal payload 直接调下一层 bypass 当前调试层,20 行代码 + 5 分钟把 N 档收敛到一档;含 CI runner 形式盲配套——多后端/多平台首次真机 execute 必配真机 runner)+ **§7 诊断侧对偶**:退化数字 ≠ 慢在你以为的路径,收到「某条路径导致 N 倍退化」类归因、动手写止血/修复计划前,先 grep build tag / 读函数头注 / 跑白盒探针证该路径在当前二进制里真的存在且可达 + **§8 度量侧**:加速数字 ≠ 机制在稳态生效,读投机/inline 快路径的收益或复查历史 perf 裁决时,度量单位必须是跨 Run 稳态的 exit-reason dispatch 计数而非单个 Run 内命中数——选错度量单位会把「从没生效的优化」读成「生效但太贵」(issue #67 amd64 NodeHit:烤进段的 TableRef 身份 guard 跨 Run 100% 落空,「命中 17 万次」是单 Run 假信号,PR #74 arm64-only 裁决被证伪;修法换 hmask 边界 + nodeRef guard 使跨 Run 重建的同 shape 表真命中)。聚合 PW5 inline-proof / PW6 TierStuck no-op / PW9 vararg 空测 / PW10 R3 错误路径盲区 / PW10 R1-R2 工作负载错配 / PW10 ③b 快路径命中盲区 / VS0-e 覆盖度先验证 / PR #28 darwin/arm64 bypass 探针 + CI runner 形式盲 / issue #40 arm64 归因证伪(诊断侧对偶,§7) / 三个 fuzz 探索空间维度实例(§5) / issue #67 auto-mode 测试静默退化(未强制测试悄悄滑向纯解释器,解药 `PromotionCount>0` 兜底断言) / issue #67 amd64 NodeHit 跨 Run 度量单位(§8,单 Run 命中数 vs 跨 Run 稳态 dispatch,解药 `TestPJ10_TableNodeHit_CrossRunInline` 断言每 Run dispatch < 500)**十四个独立实例**(跨过 ≥3 实例阈值多次);另 issue #91 顶层 diamond 修复配白盒 `TestP3_TopLevelDiamondPromotes` 断言 `PromotionCount>0` 是 §2(b) 解药的又一应用(byte-equal 分不清真升层与静默 CFAIL 回退,这个能力级 bug 藏这么久正是缺这条正交证据)。与 [[design-claims-vs-codebase-physics]] 构成「实现前重验 / 实现后证明路径」对偶双防线,与 [[perf-optimization-workflow]] §1/§3 配 perf 数字完成三关。**写差分 / 差分测试 / 性能 / IC 快路径 / wasm 快路径 / 错误冒泡类任何对路径执行做断言的测试前、扩接受面 / 换硬件 / 改 fuzz 参数后要不要立刻跑 fuzz 时、调试机制叠加多档崩点时、收到退化归因准备写修复计划前、写 auto/自然触发路径测试不带 force-all 时、读投机/inline 快路径收益或复查历史 perf 裁决时看这篇。**
- [[backend-capability-vs-profitability]] — 后端接受面按能力/收益分层:capability(`SupportsAllOpcodes`——能不能编,正确性)/ profitability(`WorthPromoting`——编了赚不赚,性能)/ 尺寸地板(`MinPromotableLener`——固定 Run 成本摊薄)/ 地板豁免(`FloorExempter`——地板的 per-proto 反向门,issue #67:proto 虽短于地板但热路径派发通道不付固定 Run 成本,P4 用 `ProtoSeg2SegEligible` 回答,裁决缓存 `ProfileData.floorExempt`;豁免裁决只问一次不重问,不豁免裁决在 `OnBackEdge` 预热里程碑(回边计数 ==1 / ==HotBackEdgeThreshold)重置重问一次,防冷 IC 时的临时裁决把 proto 永久钉死解释器)四类 per-backend 可选接口,bridge 侧 `considerPromotion` 依次咨询;**forceAll 只绕收益类不绕能力类**(差分测试覆盖面由能力层决定,不因收益判断缩水;地板豁免不适用于 forceAll——forceAll 本就绕地板),收益拒绝 → TierStuck 吸收(静态判断重复问不变);四实例——P4 amd64 CALL 密度门(`totalOps/callCount>=16`)+ P4 `MinPromotableLener` 尺寸地板(fixed Run ~111ns > 解释器 78ns,tiny proto 停 10)+ P3 wasm helper 密度地板(`total/helperBound>=7`,nbody 43.5→89.7ms 回归驱动,issue #39)+ P4 `FloorExempter` 地板豁免(spectral-norm 热 proto `A(i,j)` 9 op 被地板永久钉死解释器,arm64 auto 16.6ms 慢 force 3.7x,issue #67)+ **P3 wasm `WorthPromoting` back edge 维度(issue #92:直线小体无循环摊薄边界往返,`straightLineMinCodeLen=32` 拒,收益门家族第 5 实例,判据从 op 密度扩到结构维度)**;能力层放行 ≠ 承诺 auto 升的正面样本:**issue #91 relooper 放行顶层 diamond 让 fannkuch 主函数第一次能升 P3(纯能力层),升了也就解释器水平(force 6.20ms vs P1 5.91ms),auto 仍被 CALL 密度门拒——两层分开修、分开量**;工作流「新后端/新 op 接入先分类:编错 → capability / 编了净亏 → profitability / 短 proto 摊薄不回 → 尺寸地板 / 短 proto 但走段内派发不付固定 Run 成本 → 地板豁免」+「收益阈值校准 = 白盒 op 构成统计先行 + 实测迭代边界样本」+「共享分析器改动后每后端独立复测」+ **§4「互补 PR 合入顺序:收益门先合」(能力修复放行新形式 + 收益门防反噬分两 PR 并行时过滤门必须先合,防「能达但没兜底」回归窗口,实证 #91/#92 PR #100 先于 #101)**。**§6 记录该四类接口的测试覆盖现状**:全部只在 auto 模式生效、长期缺 auto 专属测试网,issue #67 auto-mode 覆盖轮(PR #75)补上 `Bridge.SetHotThresholds` testing-only 入口 + 四类新覆盖套件;**并记「升层判据每加一维,热度类 fixture 必须跟着满足全部维度」纪律**(issue #21 尺寸地板 ≥10 op + issue #92 back edge 两实例,`TestPromotionCount_P3_NoForce_HotEntry_Lifts` 被两次改造,连锁失败是门在工作的正信号非回归)。**给某后端接入新 opcode / 新形式时、某后端 auto 模式性能回归但 forceAll 正常时、共享可编译性判据改动后每后端复测出现分歧时、写「按 shape 拒收」类升层门时、能力修复与收益门分两 PR 并行定合入顺序时、改升层判据后热度类 fixture 连锁失败时、想知道 auto-only 接口测试覆盖现状时看这篇。**

### memory/
- `memory/doc-gaps.md` — 已识别的文档缺口与待外部确认事项(随实现推进收敛;含已收口审计记录)。
- `memory/decisions/2026-06-11-design-review-decisions.md` — 设计评审轮 7 项裁决归档(验收 oracle 改官方 5.1.5、stdlib 反转对齐 gopher 提供面+三层禁用、ColInt64 超界报错、luac 同构软承诺等),每项含设计文档落点指针。**查「某决策为什么这样定、落在哪」先看这篇。**
- `memory/reflections/2026-06-11-design-doc-completion.md` — 设计文档集补齐(P1 全卷 + P2-P5)的过程反思:并行起草+单点收口模式、子代理中断恢复教训。
- `memory/reflections/2026-06-11-design-review-round.md` — 设计评审决策轮过程反思:主动盘点不确定决策的收益、裁决后即时 grep 同步、AskUserQuestion 自包含教训。
- `memory/reflections/2026-06-12-p1-implementation-sprint.md` — P1 实现冲刺(M8-M14 单会话收口)过程反思:difftest 后置的代价(5 个单测漏掉的语义 bug)、lcode.c 同构须到 helper 层、ci 指针刷新不变式、「简化实现+接口留口」模式。**做实现冲刺或 P2 接 difftest 前看这篇。**
- `memory/reflections/2026-06-12-p1-closeout-round.md` — P1 收尾轮(「已知简化」9 项全量完成)过程教训:对称机制复用通道(yield 借 error 哨兵)、IC 命中必须验同键(动态 key 指令)、Program 运行期可写字段须 State 私有化规则、生成器类型封闭纪律。**P2 实现 IC、给 Program 加字段、或写受控文法生成器前看这篇。**
- `memory/reflections/2026-06-12-test-hardening-round.md` — 测试加固轮(go-fuzz 目标/生成器两期/GC 压力/错误消息差分测试/nightly)过程教训:每类 fuzz 上线当天即抓到 5 个真实 bug——fuzz 目标空转(脚本在跑但无 `func Fuzz*`)是最危险的虚假安全感、「末位多值源 A 处理」同族 bug 三处分布证明同构逻辑须抽 helper、top 恢复纪律(`L->top = ci->top`)是 5.1 调用约定的一部分且症状离根因极远、GC 压力模式让正常模式难触发的时序 bug 必现(弱表链截断实例)。**搭新防线、写调用桥、或评估「防线是否真在防」时看这篇。**
- `memory/reflections/2026-06-12-completeness-gap-round.md` — 完整性补全轮(特性探测 corpus + 25 缺口修复)过程教训:**差分 fuzz 两轴正交模型**——随机生成器文法跟实现走,只测「已实现行为的正确性」,对「缺特性」结构性失明;特性探测 corpus 按官方手册逐节写,测「特性面完整性」;probe 上线在 570+ 随机脚本全绿下一次扫出 25 个缺口。另含:probe 先过 oracle 纪律(两例笔误)、goIfTrue/goIfFalse 恒跳丢值 bug(`nil and 2` 错产 false)、pattern 灾难性回溯有界失败裁量、「probe 转绿 → 进生成器文法」护栏闭环。**设计新防线选参照系、评估 diff-fuzz 覆盖含义、或 P2+ 接新执行层时看这篇。**
- `memory/reflections/2026-06-12-longevity-review-fix-round.md` — 长稳与审查核销轮(freelist 复用 + 12 轮外部审查 22+ 项全核销)过程教训:**三轴防线模型**(fuzz=行为自洽 / probe=特性面完整性 / review=规范同构+形式组合,前两轴全绿不构成第三轴可省略的理由)、**同构须到时序层**(constFold 须在 exp2RK 之后,操作顺序本身就是规范)、**注释承诺审计**(注释声称的防线不存在就是 bug 预告,四实例)、**内存复用类变更的「良性→致命」升级清单纪律**(复用前先列哪些潜伏 bug 会从良性变 UAF,根审计/尺寸单一源/debug 设施同批)、修复轮收尾的历史 bug 保护测试盘点。**做资源复用类变更、引入外部审查、或写 C→Go 移植代码前看这篇。**
- `memory/reflections/2026-06-12-official-suite-perf-round.md` — 官方测试套与性能轮(luasuite 移植扫出 20 项分歧全修 + profile 驱动六项优化 + IC DataOff 实测回退否决)过程教训:三轴升级为**四轴防线模型**(官方套=作者语义断言,独有负断言能力)、stopAt 棘轮机制、profile 先行(最大收益项 closeUpvals 不在预判清单)、benchmark 否决门 + 快 revert、归因诚实(9.0x 主因是 thread 复用消固定开销)、快路径家族审计(cbaae3f 反例)。**做性能优化、接官方测试套、或评估防线覆盖时看这篇。**
- `memory/reflections/2026-06-12-issue1-api-gap-round.md` — issue #1 公共面 API 缺口轮(per-item drop-in 子集 + Register/Module + 公共 HostFn + Value.kFunction + pin 表 GC 根)过程教训:**「设计承诺源回看」纪律**(issue 字面边界与 issue 描述的业务场景不自洽是常见根因,从设计承诺源反推真实最小可用集)、**宿主长持 GCRef 必须接 GC 根**(first-class function value 暴露的对偶面,pin 表/`visitExtraRefs` 接 R8 类常驻根,Release 可选但根必须有)、**单域提交的物理隔离手法**(一锅写完后按文件抽取再 git add 分次,比 git add -p 选 hunks 更可靠)、**错误消息稳定语义纪律**(不带里程碑编号/hash/时态承诺,公共面措辞与实现状态解耦);附带短评:**主库 go.mod 零外部依赖**(benchmark oracle 依赖按用途拆独立子模块,`d1ff096`)。**两条 promotion 候选(增量交付 guide 立项、GCRef 接根升 reference 不变式)已在 issue234 反思轮定下来完成。**
- `memory/reflections/2026-07-14-issue56-api-gap-round-3.md` — issue #5/#6 公共面 API 缺口轮 3(Table.ForEach + globals baseline 状态隔离)过程教训:对称面缺口延迟发现(写入/读出闭环检查)、GC 根两条通道(pin 表 visitExtraRefs vs baseline visitExtraValues)、线性扫描量级显式注明、一样的工作流三轮零阻塞验证 guide 已稳定。**promotion 已完成**:[[public-api-incremental-delivery]] 第 9 条对称面检查 + [[embedding-contract]] 不变式段补 baseline GC 根指针。
- `memory/reflections/2026-06-12-issue234-api-gap-round-2.md` — issue #2/#3/#4 公共面 API 缺口轮 2(Table / HideFileLoaders 沙箱 / Context 取消钩子)过程教训:一样的工作流第二轮验证「设计承诺源回看 / GCRef 接根 / 单域物理隔离 / 错误消息稳定」四条纪律可复用(尤其 kTable 复用 kFunction 一样的 pin 表零额外成本,验证机制级通用),**新增四条**:**行为变更显式标注**(`fromInner → fromInnerWithPin` 静默扩面被评审抓出,`bb1e9a8` godoc 补 v0.1.1→v0.1.2 行为变更段,是错误稳定语义纪律的对偶面)、**范围扩张顺手收口**(发现上轮裁口阻挡本轮 spec 时现场收掉比留 trick 跨 issue 强 100 倍,但 commit message 必须显式标注)、**对位测试断言先 grep oracle**(PUC 5.1 实际错误带变量名前缀,印象写法反而验证了实现已对齐官方)、**internal 接口签名避免反向依赖标准库**(`SetCancelHook(func() error)` 抽象签名留在门面层注入 context,保持 internal 零标准库非基础包依赖,P3+ JIT 一样的机制可共用)。**两条 promotion 已完成**:[[public-api-incremental-delivery]] guide 立项聚合 8 条纪律 + [[embedding-contract]] 升级公共 first-class GCRef-bearing value 接 GC 根为契约级不变式。**做公共 API 演进对外披露评审、规划「相对前版静默改了什么」时看这篇。**
- `memory/reflections/2026-06-13-drift-audit-and-fuzz-hardening-round.md` — 累积偏移审计 + fuzz wall-clock + 嵌入式 hardening 轮(`bb1e9a8..701b1a4`)过程教训:**① 累积偏移审计纪律**——同一驱动源接入到第 N 轮后必做横向审计(本期 6 issue 跨 3 轮),与官方套轮「快路径家族审计」([[official-suite-perf-round]] 做对了 1)同「同族审计」纪律家族(前者性能优化扫同族快路径,本条公共 API 演进扫同族累积偏移);**② 审计具体完成手法**——主导判断而非外包子代理(累积偏移是判断题不是搜集题)、维度 AB 划分(违设/别扭)、严重度三档分级(本轮收/P2+ 入档/建项目纪律)、留「没有违设、看似别扭其实是正解」节防审计偏负面;**③ 测试机制依赖底层框架的时间维度假设**——fuzz wall-clock(`-fuzztime`)与 `SetStepBudget` budget 不同步,seed 上限必须远低于 fuzztime 量级(`SetStepBudget(1<<20)` 下保守取 `1e5` 而非 `1e6`),与「指令计数不能替代 wall-clock 事件触发」同原理(量级层不可跨界假设);**④ 真 bug 由 flake 排查顺手抓出**——修 flake 的副作用(改 seed/阈值/采样率)等价于覆盖路径变化 = fuzz 新一轮探索,本期 1e9→1e5 紧边界 mutate 距离更短抓到 `string.rep("k", 1e14)` 真 OOM,机制级而非巧合;**⑤ 嵌入式 hardening 阈值类**——主动偏离对位 backend 的特殊关系(底线优先级「宿主进程不可崩 > 与对位字节一致」),不同于豁免(合法的不比对)、不同于设计差异(语义有意不同),阈值口径分配类 1 GiB / 循环类 1<<24 + 引入条件三条落 `12-testing-difftest.md §4.9`,与长稳轮「内存复用配套清单」同属「承诺优先级冲突的明确裁判机制」家族。附:`make all` 默认扩面(`44dc6f8` 三件套 → 七件套,工程纪律选择)。**触发场景**:做累积偏移审计 / 写 fuzz/budget 类测试 / 设计 hardening 阈值 / 一样的 issue 接入多轮后想暂停做横向审计时看这篇。
- `memory/reflections/2026-06-13-issue8-boundary-cost-round.md` — issue #8 边界成本轮(`CallInto` 零分配 + 四档真实世界 benchmark)过程教训:**① 实现浪费 vs 架构成本辨析**——性能 issue 第一分类问题,边界双拷贝(72 B/2 allocs,VM 栈→inner→public)是可消除的实现冗余而非 NaN-boxing/arena 架构成本,前提一能判否优化提案但不能掩护实现浪费(尤其首个目标宿主真实热路径 + 已承诺 drop-in 对标场景);**② 零拷贝复用栈返回值的双重验证**——GC 根可达性(runningThread 复位后 mainTh 仍常驻根,`SetGCStressMode` + string 返回值 500 轮无 UAF)+ 覆写契约(返回值底层复用栈、下次进 VM 前消费完,与旧 `Call` 独立拷贝可长持的根本差异,godoc ⚠️ + 串台/独立性测试都留),与长稳轮「内存复用配套清单」同家族;**③ benchmark 必须覆盖真实交互形式**——「VM 不是自 high 产品」,旧基准全不跨界致 9× 头条是嵌入者够不到的天花板、真实热路径劣化不可见,固化四档(纯VM micro / 真实负载纯VM / 边界 mini / 真实负载 embedded),边界档并列 `Call` vs `CallInto` vs gopher 让问题与修复同屏可验;**④ README 性能数字同机同日整节重测**(本轮 simple 9.0×→5.9× 机器差异,四档自洽)。**promotion 候选**:教训 1（实现浪费 vs 架构成本）+ 教训 3（benchmark 覆盖真实形式）候选进 [[perf-optimization-workflow]],首次样本暂留观察。**触发场景**:收到「某形式比对标对象慢」的性能 issue / 做返回内部缓冲区切片类零分配优化 / 给嵌入式 VM 设计或评估 benchmark / 改 README 性能数字时看这篇。
- `memory/reflections/2026-06-13-p2-doc-expansion-round.md` — P2 单文件设计稿扩展为子目录详细设计的过程教训(`p2-bridge.md` 703 行 → `p2-bridge/` 8 文件 7453 行,multi-doc-drafting 二轮验证):**① multi-doc-drafting 三协议二轮零阻塞**——并行起草 + 单点收口 + 回填请求节 + 唯一验收口径同 P1 19 篇起草轮验证有效,一样的工作流第二次跑零阻塞,验证机制级稳定;**② 子代理工具卡死的 Bash heredoc 兜底**(新教训)——子代理 Write/Edit 卡死但 Bash 可用,自救协议「`cat >> file <<'EOF' ... EOF` 续写」是救命招;主助理「停子代理 + 派新子代理续写」是合法救援动作,介于 multi-doc-drafting guide 「同篇连续失败 2-3 次主助理亲写」上限之前的中间档;**③ 拆子目录前必须做章节映射表**(新纪律)——单文件 → 子目录拆分时,带 §X 章节号的引用不能批量指向总览(章节不存在 = 断链),必须按章节映射(§3 IC→02-ic-feedback、§4 可编译性→03-compilability 等);**④ 子代理定下来 + 用户裁决**——子代理保守(YAGNI)+ 用户偏激进(预设接口零成本)是健康协作:本轮 AST 协议子代理选 ① 用户采纳;profile 归属子代理选 (B) 用户改 (B+C) 嵌套预设 (C) 接口避免事后改接口。**promotion 候选**:教训 2(Bash heredoc 兜底)+ 教训 3(章节映射表)候选进 [[multi-doc-drafting]] guide,首次样本暂留观察。**触发场景**:做大型文档起草扩展 / 子代理 Write 卡死 / 单文件 → 子目录拆分 / 多人协作下的不确定决策收口时看这篇。
- `memory/reflections/2026-06-14-p3-pw5-table-ic-round.md` — P3 PW5 表 IC opcode 翻译轮(翻译复杂度峰值,四提交 `bb3f16f..5c181b1`)过程教训:**① 设计稿热路径上的 `(call $helper ...)` WAT 伪码是记法不是承诺**——`02-translation.md` §3.4 把 `$ic_slot_load`/`$ic_key_match` 写成助手调用,但 PW0 实测一次跨层 imported 调用 ~143ns,快路径上一调「跳哈希」省的几 ns 立刻被边界成本吞光、IC inline 收益归零;正解是全 inline `i64.load`——table 活在 arena=wazero linear memory、`GCRefOf` 低 48 位即字节偏移、`SNAP_INDEX` 是编译期立即数 ⟹ 所有槽 offset 都是常量,复用 VS0「偏移现算寻址」同一条物理洞察;**② inline 覆盖按「逐字节可证性」分级而非「能不能做」**——只 inline 可证逐字节一致的形式(常量键:同表同代次 ⟹ 键匹配整段跳过,顺手解了字符串常量键值烧不进 Wasm 的难题;寄存器键 ArrayHit:`f64(key)==Index+1` 绕开 i32 截断陷阱),刻意路由到助手的脆弱情形(NodeHit/MonoMeta `__index`/带字符串常量值的 SETTABLE),no-compromise 的真实含义是对主导情形交付验收口径而非穷尽 inline;**③ 守卫多的路径用「两块结构($done/$slow)+ 恒定 br 深度」**胜过深嵌 if 的 br 深度算术(br 深度与检查数解耦,加减守卫不必重算);**④ 证明「跳过了哈希」的 inline-proof 测试需毒化/哨兵助手**——普通 e2e 无法区分 inline 快路径与助手回退(两者都正确),用「毒化助手返回 error」或「哨兵不写 `R(A)`」让两条路径在断言上可分辨,这是验证验收口径本身(路径被走到)而非只验正确性的唯一手段;**⑤ Edit old_string 吞掉相邻 func 声明**——在已有声明正前方插代码时锚点骑边界,留下无宿主孤立函数体。**promotion 候选(首次样本,暂留观察)**:教训 1(设计稿热路径 `$helper` 须按边界成本预算重判,P3 翻译全程复发,可并入 [[issue8-boundary-cost-round]]「实现浪费 vs 架构成本」对偶面)+ 教训 4(inline-proof 须用毒化/哨兵助手,P3 分级 inline 全程通用测试技术)。**触发场景**:接 P3 后续翻译里程碑(PW6/PW7)、照抄设计稿 WAT 伪码前、做「快路径 inline vs 退助手」分级裁量、写「证明某条快路径被走到」的测试、或在已有函数声明正前方用 Edit 插代码时看这篇。
- `memory/reflections/2026-06-14-p3-pw6-crosslayer-call-round.md` — P3 PW6 CALL/TAILCALL/RETURN 跨层互调轮(P3 最大里程碑,三提交 `6546e45..eaab36d`,gibbous→{crescent,gibbous,host} 三向分派)过程教训:**① 设计稿热路径里跨层传递的固定 token(此处 `$base`)实现前须拿本码库内存物理重核,否则藏 codebase 专属 UAF**——`04-trampoline.md` §2.2 把 `$base`(linear memory 字节偏移)画成入口一次性收到全程不变,但 gibbous 帧经 `h_call` 调更深 Lua 帧时嵌套 `growStack` 会把值栈段在 arena 重定位、改 `stackBaseW`,返回后陈旧 `$base` 指向已 Free 旧段 = UAF;解释器经 `slot()` 现算免疫,但 `$base` 是 wazero local 中途无法自刷新——这是 VS0 形式 Y 别名雷区的 gibbous 帧对偶;解法是 `h_call`/`h_tailcall` 返回重算的新 base(i64,负哨兵表错)而非 status,`local.tee`→负则 `return 1` 冒泡/否则 `local.set $base` 刷新(插桩测实证 helper(100) 深递归 stackCap 64→256 段确搬家);**② 慢路径复用解释器换三重免费红利**——CALL/TAILCALL 路由回 crescent `doCall`/`doTailCall`/`executeFrom`(gibbous→gibbous 也降级解释器跑,gibbous 仅优化层):(a) 逐字节一致按构造成立、(b) proper TCO 拿 O(1) 栈免费(`f(1e5,0)` 不溢出)、(c) 错误冒泡+pcall 清理透明(gibbous 帧 CallInfo 形式同 crescent,`ciTop` 回退零特判),是「解释器永不退役」作正确性基底而非仅 fallback 的兑现;**③ 一个 opcode 既当 BB 终结子(多 BB)又当单 BB 直线末指令时两条发射路径都要处理**——TAILCALL 后仅死代码 RETURN ⟹ `reachableBlocks==1` 走单 BB 直线路径 `emitOpcode`(当时无 TAILCALL 分支 → default「未实现」→ 静默回退),RETURN 早有的 `emitOpcode`+终结分派双处理本是现成模板,tail-shaped(`return f()`)/trivial-leaf 是独立 CFG 形状须专测;**④ tier 升层 e2e 深 baseline 自升进 TierStuck 吸收态**——深循环/递归 baseline(尾递归 n=1000)在 baseline 期触自然升层阈值 → `considerPromotion` 走 `NotCompilable`→`TierStuck` 永久卡死 → 后续手工 `promoteProto` 成 no-op(返 false→静默 skip),修复是 oracle 与 gibbous 各用独立 State + 深跑前先 promote(与「绿色≠在测你以为在测的」同源)。其它:HostState `Call`/`GetGlobal` 等撞既有 `State` 公共 API 改 `Do*`/`*Raw`(PW5 一样的复发模式)、CALL B=0/C=0 与 TAILCALL B=0 多值窗口需跨 opcode 维护 top 故保守回退、`h_call` 返 i64 单值兼载新 base 与错误负哨兵。**promotion 候选**:教训 1 **已是该家族第二实例**(PW5 边界成本维度 + 本轮段重定位内存物理维度,使其从首次样本升为真实模式),**建议从 memory 提升**——可立 guide「设计稿主张须对本码库 physics 重新验证」聚合 [[issue8-boundary-cost-round]]+[[p3-pw5-table-ic-round]] 教训 1+本轮;教训 4(tier 升层 e2e TierStuck 吸收态陷阱)首次样本暂留观察。**触发场景**:接 P3 后续里程碑(PW7-PW9)、照抄设计稿把热路径值画成跨层固定 token 前、决定「加速层重写 vs 路由回解释器」、接兼具 BB 终结子与单 BB 末指令双重身份的 opcode、写含深循环/递归 baseline 的 tier 升层 e2e、或给 HostState 加镜像 opcode 名方法时看这篇。
- `memory/reflections/2026-06-14-p3-pw7-pw4b-closure-tforloop-round.md` — P3 PW7(CLOSURE/CLOSE/VARARG)+ PW4b(TFORLOOP 泛型 for)翻译轮(本轮后全 38 opcode 除 VARARG 外全可翻译,两提交 `6f2fd0e`/`aec94c6`)过程教训:**① 设计稿标注的「难点」可能已被前序里程碑解掉,动手前先核实它是否仍是难点**——设计稿 §3.7 给 PW7 估 0.5-1 人月并把「开放/关闭 upvalue 在 linear memory 形式下的存储协议」标为核心难点,但该难点早被 VS0-c(值栈 arena 迁移)解掉(open upvalue 经 `owner.slot(idx)` 形式 Y 自动重定位 + gibbous 侧 upvalue 访问从 PW2 起走 helper)⟹ CLOSURE/CLOSE 仅路由 helper 复用 `makeClosure`/`closeUpvals`、无新协议,真正非平凡点反是设计稿当脚注的「跳过 CLOSURE 后随伪指令」——难点发生**时间维度漂移**(基础变更追溯性溶解后续里程碑的难点,设计稿不会自动更新该判断);**② `emitOpcode` 的 `(skip,err)` 重构让发射循环跳过 opcode 后随的伪指令数据字**——CLOSURE A Bx 后紧跟 `SubNUps[Bx]` 条 MOVE/GETUPVAL 伪指令(是数据非可执行 opcode),两条发射循环(单 BB 直线 + `emitBlockBody`)按 pc 迭代会误译成寄存器拷贝,解法 `emitOpcode` 返回 `(skip,err)` + CLOSURE 返回 `Proto.SubNUps[Bx]`(注释「symbexec 精确跳过用」天生为此设计)+ 循环 `pc += 1 + skip`,且**先做纯机械签名重构验证 PW1-PW6 零回归再加 CLOSURE 分支**(机械改 vs 新语义混一 diff 则回归不可区分),与 PW5 SETLIST C=0「下一字是数据」同物理现象的两种应对(那里回退/这里跳过);**③ TFORLOOP base 刷新是 [[design-claims-vs-codebase-physics]] §2 第 4 实例且这次在 planning 阶段就预判到**——TFORLOOP 迭代器调用走 `callLuaFromHost` 可 growStack 重定位值栈段 ⟹ 返回后 `$base` 陈旧 UAF(PW6 `h_call` 同雷区),因 guide §2 已存在、规划阶段即预判 → `h_tforloop` 返回从设计稿 3 值状态(0/1/3)改为 i64 同载刷新 base 与 3 状态(newbase≥0 续/-1 ERR/-2 退),教训成本在规划时一次付清而非再经 GC 压力失败重新发现,**是 guide 提升(PW6 轮)的正面确认而非新 promotion**;**④ TFORLOOP/CLOSURE 复用解释器跨层机制是 PW6「慢路径复用解释器」红利的延续**——TForLoop 复用 `callLuaFromHost`(被升层迭代器自动经 PW6 `doCall`→enterGibbous 再入)/Closure 复用 `makeClosure`/Close 复用 `closeUpvals`,PW6 跨层机械使新控制流 opcode 几乎零增量(复用基底会复利、每里程碑降低下一个成本)。其它:VARARG 不需发射代码(不在白名单→`SupportsAllOpcodes` 假→回退,设计稿「Wasm unreachable 防御翻译」多余因白名单已在 emit 前挡住,`TestPW7_VarargRejected` 锁死)、TFORLOOP e2e 用自定义 Lua 迭代器(crescent 无 stdlib 否则 import cycle)走相同 `R(A)(R(A+1),R(A+2))` 路径、一样的 TierStuck-on-deep-baseline 陷阱(独立 State + 深跑前先 promote)、i64Const 负哨兵用 `^uint64(0)`/`^uint64(0)-1`(`uint64(int64(-1))` 是 Go 常量溢出编译错)、两个机械 Edit 失手(heredoc EOF 痕迹 / 游离 `}`)被 build 抓到(PW5「Edit 吞声明」同族)。**promotion 候选**:教训 1(设计稿「难点」可能已被前序里程碑解掉,动手前重核)首次样本暂留观察,PW8 线程级 tier 规则难点可能也已被 `th==mainTh` 守卫部分解掉,若再现可并入 [[design-claims-vs-codebase-physics]] 作「时间维度」补充(physics 不只空间不变式,也含「前序里程碑已改变的事实」);教训 3 是 guide 正面确认非新 promotion。**触发场景**:接 P3 PW8/PW9、开始任何标注「难点」的里程碑前(先核实难点是否仍在)、给 opcode 加后随数据字的翻译、热路径值穿过可能 growStack 的助手时、写 tier 升层 e2e(深 baseline TierStuck 陷阱)时看这篇。
- `memory/reflections/2026-06-15-p3-pw9-acceptance-perf-round.md` — P3 PW9 端到端总验收轮(正确性轴交付 + 性能轴空测翻案,五提交 `bb39b06`/`e94a80e`/`f556c19`/`419e278`/`ed0375a`)过程教训:**① 空测陷阱(vacuous-benchmark):tier-vs-tier 基准不证明被测层真被走到就是 0 信息**——loop 核基准测得 gibbous≈crescent(≈1.0x),据此推出「memory-resident 流量相同、dispatch 消除不足 2x、须上 locals 缓存」并准备把性能轴当**根本限制**拆里程碑(`bb39b06` commit message 已写入),但那 `for` 写在**顶层 vararg chunk**(F1 结构性排除永不升层)⟹ 实际测的是 crescent==crescent,≈1.0x 是构造性必然非发现;修正后真实数 **2.58x**(crescent 5.61ms vs gibbous 2.17ms,V14 ≥2x 立刻达标)。差一点把**本就过线**的性能轴叙述成「memory-resident 根本限制」并立**错的后续里程碑**(locals 缓存——与真瓶颈正交)。原则:**加速路径 `≈1.0x` 是红旗不是发现**(默认「没走加速路径」),任何 tier A vs B 基准读数前必须先证 tier B 真被执行(把 kernel 包进可升层载体非顶层 chunk);**② 非空断言测试是解药,且这是 PW5/PW6 之后同家族第 3 个实例——「绿色 ≠ 在测你以为在测的」已跨过提升阈值**——修复不是「再跑一遍」而是经已证非空路径重测(kernel 包进非 vararg 内层函数反复调用使过热度)+ 白盒 `TestPW9_ForceAllPromoteReal` 经真实公共 force-all 路径断言 proto **确实到达 `TierGibbous`** + `TestPW9_ForceAllRespectsStructuralGates` 断言 vararg 即便 force-all 也不升层;三实例(PW5 inline-proof 毒化助手 / PW6 TierStuck no-op 静默 skip / PW9 vararg 不升层)共同形式是**静默替身路径**产出「看起来对」的结果把真要测的路径挤掉;**③ 编译期 F7 是「无后端注入」条件下算出的历史占位非能力陈述,检查裁决须在条件匹配处重判**——编译期 `analyzeCompilability` 用临时 throwaway Bridge 跑 F7 时 `b.p3==nil` ⟹ F7 恒触发烧成 `CompNotCompilable+ReasonBackendUnsupp`,force-all 仅绕热度则一个 proto 都升不动;`recheckCompilabilityForce` 在 forceAll 下对**真实注入的后端**重判 F7、**保留** F1-F6 结构性排除(已 AST-free 烧进 `proto.CompReasons`,`ReasonsBitmap &^ ReasonBackendUnsupp`),即「绕编译期 F7 占位、不绕真实 F1-F6 检查」;**④ 真瓶颈是跨层调用税非 memory-resident 寄存器,原计划 locals 缓存正交治错了成本**——修正后真实画面:loop 2.58x / mixed 1.60x 过线,但 table 0.68x / call 0.14x(慢 7 倍)调用密集核退化,根因 gibbous→gibbous 经 `h_call` 双跨层(Wasm→Go→Wasm,PW0 实测 ~143ns/次,30 万次小叶调用 ≈ 43ms 纯边界税),是 `must/design-premises` 前提一/前提二边界成本物理的运行期实证;geomean 一个总数会抹平双峰(赢的核/亏的核),须按 kernel 形式分桶看;**⑤ 里程碑级架构改动配 spike 检查,绝不盲目重写核心假设未验证的方案**——跨层调用税真解是 gibbous→gibbous 直接分派,被两条码库 physics 挡住(每 Proto 独立 wazero module ⟹ 跨 module 必穿 host;Lua 帧活在 Go `th.cis` ⟹ 帧设置需 host 除非 CallInfo 迁 linear memory=VS0-e),**生死未知数**是 wazero 能否往已实例化 module 追加函数(当前证据:不能);决策**不盲目开干**——用 spike 检查(对齐 PW0 precedent)先验 S-A intra-module `call_indirect` 成本(<30ns)+ S-B 增量 module 重编生命周期,绿则重写、红则退守「拒升小叶函数启发式」(预写红灯退路)。**promotion 候选**:教训 2(非空断言:绿色 ≠ 在测你以为在测的)**第 3 个独立实例已跨过提升阈值,强烈建议提升**为测试类 guide(workname:`prove-the-path-under-test`),聚合三类静默替身路径反模式 + 解药(毒化哨兵助手 / 正向 tier 断言 / 非空载体+路径断言),或并入 [[test-hardening-round]]「fuzz 目标空转」框架——它与 [[design-claims-vs-codebase-physics]] 构成「实现前重验主张 / 实现后证明在测路径」对偶双防线;教训 1(空测陷阱)随教训 2 一并进 guide(差分套 + 基准都适用「先证路径,再读数」);教训 3(条件维度缓存裁决是快照非真相)首次样本暂留观察,若再现可作 design-claims guide「条件维度」补充;教训 5(里程碑级架构改动配 spike 检查)PW0 已是 precedent、本轮第 2 次自觉援用,首次以「教训」形式记录暂留观察,PW10 spike 真执行后可升 spike-gate 范式 guide;教训 4(瓶颈实测推翻模型)暂留 memory。**触发场景**:写任何 tier-vs-tier(加速层 vs 基线)性能基准或差分套时、看到加速路径 ≈1.0x 的读数时(默认「没走加速路径」)、评估一个检查/缓存/分析结果的存储裁决是否仍有效时(尤其它在与运行期不同的条件下算出)、一个性能数与你的成本模型矛盾时(先重导模型再定修复)、为架构改动在「全面重写 vs 退守启发式」间抉择时、或开始任何里程碑规模、核心可行性假设未验证的改动时(先 spike 检查)看这篇。
- `memory/reflections/2026-06-15-p3-pw10-r1-r2-callinfo-migration-round.md` — P3 PW10 消除 gibbous→gibbous 跨层调用税轮(**PW10 在飞行中**:Phase 0 spike + R1 共享 funcref 表 + R2 CallInfo 迁入 linear memory 已交付;R3 `call_indirect` 直调收益 + R4 + R5 未做,13 提交 `457559b..9d70247`)过程教训:**① 里程碑级架构改动配 spike 检查(镜像 PW0/PW9);而一个解 architecture FORK 的第二探针,能挖出比第一个被证可行方案远更简单的设计**——真解被两条码库 physics 挡住(每 Proto 独立 wazero module 跨 module 必穿 host / Lua 帧活在 Go `th.cis`),生死未知数是 wazero 能否增量提升;建 `spike/p3indirect/`(独立 module 镜像 PW0 `spike/p3boundary`):**S-A** intra-module `call_indirect` ≈2.5ns(比 ~35ns 裸 host 跨界便宜 14x)、**S-B** rebuild-all 生命周期可行(≈1.2ms@256fn),到此 spike 已绿可承诺 **Arch-1「rebuild-all」**——但它复杂(代际实例/跨代守卫/O(N²) 重编);**关键决策是没止步于「难路可行」**,补 **S-C** 去解先前一笔带过的 FORK「各 Proto 模块能否共享同一张 imported funcref 表」⟹ S-C 通过(跨 module 经共享表 `call_indirect` ≈2.5ns 零惩罚),挖出 **Arch-2「共享表」** 戏剧性地简于 Arch-1(保持「一模块一 Proto」几乎不动、无 rebuild/生命周期/O(N²));决策报告归档 `spike/p3indirect/DECISION.md`;**spike 绿灯只意味「这条具体路可行」不意味「这是该走的路」**,若存在未审视的 FORK,动手建难路前先用一个增量探针打那条更简单的分支——与 [[perf-optimization-workflow]] §3「benchmark 否决门」同构但发生在架构选型层;**② 高风险物理数据结构迁移(VS0-e CallInfo 迁入 linear memory)的「收口→并行只写影子→翻转→退役」增量剧本,并把唯一 UAF-风险翻转单独隔离一提交**——CallInfo 从 Go `[]callInfo` 搬进每线程 arena 段(4 word/帧,触及 GC 根扫描/growStack/协程栈/~108 调用点),拆成 R2a(COLD 字段收口走 accessor,行为不变)→ R2b-1(arena 段只写影子 + `wangshu_trace` 往返自检,段从不被读故行为可证不变)→ R2b-2(`growCISeg` 动态增长盖满深度,仍只写)→ R2b-3(GC 根扫描翻转为从段 READ `cl`——**唯一最高 UAF-风险步,单独隔离一提交**,夹在两个安全提交之间使故障必然归因到这一步)→ R2b-4(退役 Go slice;`currentCI` 返回稳定 `th.cur` scratch 镜像、非当前帧经 `ciAt(depth)` 值快照、`truncateCI` 解栈、varargs 留 Go 影子 `th.ciVarargs`);**③ `th.cur` 稳定地址洞见用结构性修复消解 [[design-claims-vs-codebase-physics]] §2 反复出现的 held-pointer 重定位雷区(而非靠纪律躲)**——旧 `currentCI` 返回 `&th.cis[len-1]`(指进 Go slice,append 搬家 = §2 雷区对偶:那里 arena grow 搬走 `$base`,这里 slice append 搬走 `&th.cis[i]`);`th.cur` 是地址恒定的固定 struct 字段(永不重定位),非当前帧用 form-Y 值快照(每次从 depth 重算不缓存指针),雷区被**构造性消除**而非小心绕过——是 §2 从「翻转后回传刷新值」(PW6 `$base`/PW7 TFORLOOP 纪律档)到「稳定地址 + 值快照使其无从失效」(结构档)的升级应对;**④ `wangshu_trace` 自检双职**——`verifyCISeg` 往返读回断言段与 Go 权威一致,既是真实打包正确性安全网(push 现场立即 panic + trace-tag 贯穿整套 difftest 层间套比单元往返强),又给 lint 误判「未使用」的读函数正名(绕 unused 误报比 suppress 干净);⚠️ trace-gated 自检会在重构中被无意删掉(R2b-1/R2b-4 各踩一次),重写自检点须清单化确认接线仍在;**⑤ 空测/不公平基准陷阱第 4 次复发——这次是 apples-to-oranges 工作负载错配,非「未走加速路径」**——给 4 个 benchmark 加 gibbous 列时跳出 baseline loop「慢 20 倍」(gibbous 把 kernel 包内层函数调 50 次对比既有跑裸顶层循环一次的 bench,wrapped×50 vs bare,修复加匹配的 `_WangshuKernel` 列)、arith「慢 5 倍」(call-entry 开销主导便宜算术,隔离探针证同样算术放循环里 gibbous 快 1.88x);修正后每数自洽:gibbous 计算/循环密集赢(loop 2.45x / arith-in-loop 1.88x)、调用密集输(`h_call` 双跨层税,R3 将修)。其它:`wangshu_profile` 给 crescent 加 ~28% 故 bench-all 让 crescent/gopher 跑默认 tag、gibbous 跑 p3+profile 分开(统一 tag 扭曲基线)、机械 ~108 站点转换委托 worker 配规则但协程 resume/traceback 安全关键站点亲验、新增 `make bench-gibbous`/`make bench-all`。**promotion 候选**:**教训 5**(基准公平性:被走到的路径 + 匹配的工作负载 + 惊人数字触发隔离探针)**该家族第 4 个独立实例**(PW5 inline-proof / PW6 TierStuck no-op / PW9 vararg 空测 / 本轮工作负载错配,新形式 apples-to-oranges)**强烈强化** PW9 留下的 `prove-the-path-under-test` 候选,跨过应成 first-class guide 或并入 [[perf-optimization-workflow]] 的阈值;**教训 1**(spike 检查 + 探未审视的 architecture FORK 而非只探第一可行路)PW0/PW9 已立范式,本轮第 2 次援用 + 首次贡献「spike 绿灯≠最简路径」延伸维度,接近提升为 spike-gate/architecture-fork 工作流 guide;**教训 2**(物理数据结构迁移「收口→只写影子→翻转→退役」+ 隔离单一灾难步)**VS0-a/b/c/d/e 第 5 次应用**,建议提升为迁移类 guide;**教训 3**(`th.cur` 稳定地址)建议作 [[design-claims-vs-codebase-physics]] §2 升级应对补进 guide(纪律档/结构档并列);**教训 4**(build-tag 门控往返自检双职 + 须随重构保持接线)首次样本暂留观察,可并入教训 2 迁移 guide。**触发场景**:开始任何里程碑规模、核心可行性假设未验证的架构改动时(先 spike 检查 **且** 探更简单的未审视 architecture FORK)、做物理数据结构迁移时(套「收口→并行只写影子→翻转→退役」,把唯一 UAF/损坏-风险翻转单独隔离)、引入并行镜像结构时(配 build-tag 往返自检并确保随重构保持接线)、一个重定位/held-pointer 雷区反复出现时(找稳定地址 + 值快照的结构性修复而非再加一轮「记得刷新」纪律)、加一个 tier-vs-tier 或 A-vs-B 基准列时(验被走到的路径 + 匹配的工作负载形状,与架构矛盾的数字触发隔离探针)、在会改变基线行为的 tag 下做基准时(拆开跑别让基线背它不付的税)看这篇。
- `memory/reflections/2026-06-15-p3-pw10-zerocross-stage3-round.md` — P3 PW10 零跨界 ③ RETURN 拆帧子轮(承 R3+R3.5,**PW10 仍在飞行中**:基建-a closure slot 缓存 + ③a savedTop 基建 + ③b emitReturn 守卫快路径已交付,④ CALL 建帧快路径仍待。三提交 `8aa4c02..1bff7d2`)过程教训:**① 已识别未触发风险须配套触发用例,否则规划文件留口=纸面**——计划文件标了「caller==gibbous + ciDepth≥2 触发解嵌套陷阱」的风险列表,但**未配最小触发用例**;Stage 4 difftest 全绿+性能没动的「成功」其实只证明守卫**未被触发**(快路径恒不命中),真要测的解嵌套陷阱处于结构盲区;教训不是「忘了想该风险」而是「想到了但只留在脑里/文档里没固化成测试」,纪律:计划文件每个风险标记末尾加「触发该风险的最小测试用例 = ___」,空白立刻补别留到下轮。**② difftest 快路径命中盲区(第 2 实例,跨过提升阈值)**——V1-V13 force-all 语料对 ③b 守卫**命中形式**结构盲(A/B 实证:把 gibCI resync 删掉时 difftest 仍全绿、R3 三件套也全挂),根因 difftest 只保证「输出 byte-equal」**不保证「快路径走到」**;承 PW9 [[2026-06-15-p3-pw9-acceptance-perf-round]] 教训 2(空测/未走加速路径)+ PW10 R3 [[2026-06-15-p3-pw10-r3-call-indirect-round]] 教训 4(错误路径盲区)+ PW10 R1-R2 [[2026-06-15-p3-pw10-r1-r2-callinfo-migration-round]] 教训 5(工作负载错配)+ PW5/PW6 同家族,**`prove-the-path-under-test` 已是第 5 个独立实例,跨过 first-class guide 阈值**;解药 = 「指针探针 + 命中计数器」(本轮 `TestPW9_ZeroCross_ReturnFastHit` 经 doReturnHits 计数验「helper→f 不增 = 快路径命中」)。**③ 跨机器/跨参数 perf 基线漂移**——Stage 4 一开始误判 ③ 引起 table/mixed 回归,经同 commit 同硬件同参数 git worktree 复测(3 分钟)证差异源自硬件/bench 参数而非代码;现行基线本机 Xeon 6982P 2s×3 count:loop 2.95x / table 0.88x / call 0.52x / mixed 0.99x;**纪律**:任何 perf 数字判定回归/收益前,必须同 commit 同硬件同参数复测,绝不拿 memory/reflection/implementation-progress 里历史数字直接下结论;memory/reflection 写 perf 数字时必须标硬件/参数/日期(本轮发现 index.md / startup.md / implementation-progress.md 三处均缺标)。**触发场景**:写计划文件标风险列表时(每条配触发用例)、加 Wasm 内快路径/IC inline 类「绿色不等于走到」机制时(配命中探针)、看到 perf 数字与预期/历史差异时(先重测同 commit 别先归因代码)、跨机器 perf 复现时(标硬件/参数/日期)看这篇。
- `memory/reflections/2026-06-16-vs0e-varargs-stack-underflow-round.md` — VS0-e 收口轮(varargs 进栈下区,M14 留口完成,P3 PW10 完整收尾):四子步 4 提交 `c22798b`/`4e50687`/`966318c`/`ed95020`,把 task #6 描述三项里两项已隐性交付 + 真剩的 varargs 进栈下区做完(对齐官方 Lua 5.1 真布局 `[func | vararg | R(0)..]`)。**四教训 + promotion 决策**:① 调研先于实现——延后 ≥6 个月的 task 描述常失实,本轮发现 task #6 三项里两项已被 VS0-c + PW10 R2 隐性收掉,实施前重核把范围从「2-3 小时高 UAF」缩到「2 小时低 UAF」。与 [[2026-06-14-p3-pw7-pw4b-closure-tforloop-round]] 教训 1「难点过期」同家族第 2 个独立实例,**暂留观察**,候选 落点 [[design-claims-vs-codebase-physics]] §5「时间维度」新章;② 设计文档比 task 描述更权威——P1 `05-interpreter-loop.md §8.5` 早就明确栈下区真布局,VS0-e 是完成原设计而非新设计。首次样本暂留观察;③ ciWords 4→5 段布局扩张 = PW10 ③ 收口前曾撞过的 fork(`[[project_pw10_zerocross_milestone]] §"fork 已解"`),但 PW10 预留的 wasm 端单点 `helpers_index.go ciFrameBytes` 让本次扩张改动面只有 15 行。首次样本暂留观察;④ 既有官方 5.1 luasuite 覆盖度比手写语料强 N 倍——luasuite/closure.lua 已含 NeedsArg + vararg + 协程多值 yield/resume 最复杂组合,无需再加冗余语料。**与 `prove-the-path-under-test` 候选同家族第 7 个独立实例,新维度「覆盖度正向先验证」对偶面(以往六实例都是「绿色不等于走到」反向侧)。已 promote 为 first-class guide [[prove-the-path-under-test]],本轮 ④ 落 §4「正向侧解药」节**。**触发场景**:接延后 ≥6 个月的 task 前(先重核每一项现状,描述可能失实)、task 描述与设计文档冲突时(以设计文档 + 已完成实现为准)、需要扩段布局/word 时(grep 既有 ciFrameBytes 类常量定位单点)、加 e2e 语料前(先 grep 既有官方 5.1/PUC 测试套是否已覆盖)。
- `memory/reflections/2026-06-16-p3-pw10-architectural-ceiling-round.md` — P3 PW10「按原定计划完成 P3」诚实收口轮(R3.5 之后:基建-a closure slot / 基建-b proto cache / ③a savedTop / ③b emitReturn 守卫快路径 / ④-i emitCall 守卫骨架 + fastCallHits mirror 字 / callOnStack 顶层升层 + TopLevelUplift 探针 / emit 原语 i64.add/i64.or 保留)。**三教训**:**① profile 才是合同(不是立项数字)**——立项时的数字目标(0.X x → ≥Y x)是「方向陈述」,完成中 profile 揭示「立项假设的瓶颈 ≠ 实测主导项」时须先重评目标可达性再决定继续/止损/换路径,绝不为追原数字硬上高 UAF 实现(本轮:profile `/tmp/call.prof` 证 bench kernel body 含 UnknownCall F2-b 不可升 ⟹ call 0.52x 是结构性架构边界,④-ii fast body 即使完成也只到 0.57x 仍 <1x,实现复杂度 ~200 行 wasm 字节级 codegen,**决策止损**留 followup);**② stop hook 诚实解读**——/goal stop hook 强制不结束时若 profile 证明数字不可达,收口应是「已完成子里程碑 + 文档化不可达边界」,绝不硬上 UAF 代码追数字;**③ wasm codegen 复杂度峰值需前置编排**——④-ii fast body 需在 wasm 字节级直接组帧、绑 ciTransferRef、IC mirror 写回、错误路径展开,复杂度峰值远超 ③b 守卫快路径,**前置编排成本** vs **ROI** 须在立项时就算清,不要走到「机制 80% 已铺好但最后 20% 全是 wasm 字节级 UAF 雷区」再止损。**promotion**:教训 1「profile 才是合同」已完成 [[perf-optimization-workflow]] §7「立项数字目标 vs profile 实测瓶颈」(首次以 first-class guide 立节,作 perf 判定纪律四件套之一:profile 先行 §1 + benchmark 否决门 §3 + 跨机器基线对照 §5 + 完成中止损 §7);教训 2/3 暂留反思(首次样本)。**触发场景**:perf 里程碑完成中 profile 揭示原计划某子步在事实上不可达时、/goal stop hook 强制不结束但 profile 证明数字不可达时、wasm codegen 复杂度峰值的里程碑收口决策(止损 vs 硬上)时看这篇。
- `memory/reflections/2026-06-15-p3-pw10-r3-call-indirect-round.md` — P3 PW10 R3(`call_indirect` 直调消 `code.Run` 重入)+ R3.5(host helper 改 wazero 零分配 API 消反射装箱)轮(**承 R1-R2 的续轮**,6 提交 `6f2712e..1bf9d53`:R3a 中转字基建 → R3b-1 `h_callerr` + DoReturn 写中转字 → R3c emitCall 改 `call_indirect` → R3c-fix 出错点就地标注 → R3.5 helper 零分配)过程教训:**头条——性能 spike 必须量 `allocs/op` 不只量 `ns/op`,只量时间维度会把成本归因到错误机制、让整个里程碑追错东西**:PW10 Phase 0 的 S-A 探针量了 dispatch ns(intra-module `call_indirect` 2.5ns vs 裸 host 35ns)据此把跨层税判为「~143ns 分派延迟」,R3 全部使命就是用 `call_indirect` 替掉 `h_call` 里的 `code.Run` 重入消这个延迟——R3 做完且逐字节一致后基准**纹丝没动**(call 核仍 ~6x 慢),memprofile 立刻揭穿:调用核 **1.4M allocs/op**(解释器 2),全来自 wazero `WithFunc` 的反射装箱(每跨界把每个实参 boxing 成 `reflect.Value`),边界税主导项是**分配不是延迟**,spike 漏量了分配维度;R3.5(与 R3 正交)把 25 个 helper 从 `WithFunc`(反射)改 `WithGoFunction`(零反射 stack-based)后 allocs 对齐解释器、**四核全翻面**(loop 2.65x / table 1.26x 原 0.64x / mixed 1.14x 原 0.68x / call 0.49x 原 0.17x);判据:做 spike/micro-benchmark `-benchmem` 是默认非可选,跨 FFI/host/reflect 边界默认每调用堆分配、用 memprofile 钉死。**教训 2**:perf 里程碑「机制完成 ≠ 收益交付」——加速里程碑做完(且逐字节一致)后必须用 `-benchmem` 对目标核复测确认收益兑现,数字没动是红旗 → 回 profile 重找瓶颈(本轮真瓶颈在反射装箱、R3 没碰)。**教训 3**:出错点锚定错误行号——延迟标注(攒错误到顶层用『当前帧』补行号)+ 错误路径弹帧的潜伏失配只在「弹帧的帧 ≠ 标注的帧」时浮现(R3 逐层弹帧打破解释器「错误路径不弹帧」满足的隐含不变式),`raiseGibbous` 在失败点就地标注,净结果 gibbous→gibbous 错误**逐字节等于纯解释器、严格优于旧 PW6c baseline**(是正确性改进)。**教训 4**:全成功语料的 difftest 对错误路径**结构性失明**——V1-V13 全绿没抓到教训 3 的回归,因 `runWangshuTiered` 在出错时 `t.Fatalf`(语料全是成功用例,无一例黄金输出本身是错误),强化 `prove-the-path-under-test` 家族「错误路径也要被差分测试语料覆盖」。残留 call 核 0.49x 是真实 `h_call`+`h_return` 双跨界(R4/Option B 靶子);用户在「先修零分配 helper」与「先做 R4」间显式选前者。**promotion 候选**:教训 1(spike 量时间+分配双维度)**本轮头条、强信号**,建议新增一节进 [[perf-optimization-workflow]] 或作 §1「量对维度」补充;教训 2(机制完成≠收益交付)与教训 1 配对作「里程碑收尾纪律」、或并入 [[p3-pw9-acceptance-perf-round]] 教训 4 作收尾侧对偶;教训 4 强化 PW9/R1-R2 的 `prove-the-path-under-test` 候选(贡献「错误路径须被差分测试覆盖」新维度);教训 3 可作 [[design-claims-vs-codebase-physics]] §2「延迟消费上下文相关状态 + 中途上下文被改 = 潜伏失配」邻接补充(首次样本暂留)。**触发场景**:做性能 spike/micro-benchmark 时(`-benchmem` 默认、跨 FFI/host/reflect/wazero `WithFunc` 边界默认每调用堆分配)、spike 给出「机制 A 比 B 便宜 N 倍」延迟结论并据此立里程碑时(先问 A/B 各自 allocs/op)、perf 里程碑「机制就位+测试绿+逐字节一致」后(用 `-benchmem` 对目标核复测、数字没动回 profile)、一个机制延迟消费上下文相关状态而另一机制会在延迟期改变该上下文时(就地捕获别延迟)、看一个差分/差分测试套时(问「有没有一个用例黄金输出本身是错误消息?」——若 harness 对错误 `Fatalf`、语料全成功则错误路径是结构盲区)看这篇。
- `memory/reflections/2026-06-16-issue13-parity-friendly-fastpath-round.md` — issue #13 公共面 API 缺口轮 4(typed-array `NewFloat/Int64/Bool/StringArrayTable` 四件 + `GlobalsSlot` 预解析句柄,4 提交 `452ddb6`/`0834647`/`219475a`/`0d93486`)过程教训:**[[public-api-incremental-delivery]] 9 条纪律第 4 次应用零阻塞零新增**(一样的 guide 经 issue #1 / #2-4 / #5-6 / #13 四轮验证可复用)。**两个新维度入档**:**① 跨 consumer perf 调研的「入口纪律」**——给消费侧某 issue 起草自家侧建议前,先读 consumer 自家 llmdoc 反思/decisions/doc-gaps(本轮靠先读 pineapple `wangshu-borrow-optimization-survey.md` 避开了重提 arena 列轨 -46% 这条 pineapple 已 deferred 的方案);**② parity-friendly 作 wangshu 公共 API 设计分类轴**——跨引擎 `lua_script` 字节对等是 pineapple 一类 consumer 的硬约束,把公共 API 分成 **parity-breaking**(`arena.xs[i]` 类自家专属能力)和 **parity-friendly**(`NewXxxArrayTable` 类纯 Lua 表外观 + `GlobalsSlot` 类不可见的句柄预解析),是 [[issue8-boundary-cost-round]]「实现浪费 vs 架构成本」的**消费侧对偶面**——前者评判内部成本归类,后者评判公共面对外侵入度(是否要求 consumer 改脚本)。**promotion 候选 4 项全首次样本暂留观察**(对齐 issue234/issue56 两轮才升 guide 的节奏):入口纪律 / parity 分类轴 / ByRef + opaque handle 双件套(热循环 binding pattern)/ guide 4 次零阻塞应升「实践证据」节(本轮第二次复发,建议下次再现入档)。**触发场景**:接消费侧某 issue 想给 wangshu 加 API 时(先读 consumer 反思/decisions 避免重提其 deferred 方案)、评估 wangshu 公共 API 是否要求 consumer 改脚本(parity 轴)、实现「热循环 binding」类 API(ByRef + opaque handle pattern)时看这篇。
- `memory/reflections/2026-06-17-pineapple-bench-batch-wrapper-spike.md` — pineapple §6 spike 反预期轮(batch-wrapped item-mode + DataFrame.GetColumn 完整实施 + 三 backend × 二维 BatchWrapper 实测)过程教训:**三 backend 全部反向**(预估 -17/-34/-30%,实测 +22/+27/+30%)否决 §6 方案,**两条独立致命根因**:① wangshu p3 当前 build auto-lift 路径根本不工作(`analyze_on.go::analyzeCompilability` 用临时 Bridge 无 P3,所有 Proto 编译期被烧 ReasonBackendUnsupp,运行期 considerPromotion 直接 Stuck)→ **影响所有外部 embedding 的 p3 perf 解读,不止 pineapple,本反思直接 motivate 提 wangshu 自家 issue #18**;② boundary 已被 #112/115/116 压死的形式下,把 host loop 翻进 VM 让 wrapper 内层多 7000+ opcodes × ~80ns 净增 ~560µs,反噬 boundary 节省的 ~100µs。**触发场景**:接到"机理估算 -XX%"类复合估算的优化提案前先 prove-the-path-under-test(估算只是 spike 前 sanity check 不是方案 motivation)、依赖 wangshu p3 build 自然热度升层产出 perf 数字前用 PromotionCount() 白盒验、boundary 已压死形式下评估"翻 VM 包装"类方案前算 VM 内 wrapper 自己的 opcode 净增成本时看这篇。**关联**:教训 1 直接 motivate 提 wangshu #18 + 本反思的「reflection 教训 → actionable issue」工作流在 issue #13 / issue #18 两轮验证。
- `memory/reflections/2026-06-17-issue18-p3-autolift-fix-round.md` — issue #18 P3 自然热度升层路径修复轮(承上轮 [[2026-06-17-pineapple-bench-batch-wrapper-spike]] 教训 1)过程教训:**① "编译期保守占位 + 运行期实判清洗"pattern**——`ReasonBackendUnsupp` 字面"不支持"实际是"编译期不知道运行期注入谁"的占位,`recheckCompilabilityRuntime`(原 `recheckCompilabilityForce` rename)实现运行期清占位 + 对真实后端重判逻辑,守卫双路 `forceAll || (placeholder_bit && p3_available)`;**② "白盒接通正确性"≠"宿主形式性能加速"**——白盒 `PromotionCount > 0` 证 issue #18 修复确实有效,但 pineapple 形式实测 p3 比 p1 反而慢 20%(升层后 host→crescent→gibbous wasm 边界成本 > 解释器 4-opcode dispatch),是 [[2026-06-17-pineapple-bench-batch-wrapper-spike]] 教训 3「短工作量翻 VM 反噬」家族第 2 实例,但纪律「先把事情做对再优化」、正确性补丁和性能加速补丁分开评估;**③ "留 X PR 收口"godoc 承诺当 issue 信号对待**——`analyze_on.go:31` 注释"留 P3 PR 收口"在仓库躺了 7 天没人 promote 成 issue,直到 pineapple bench spike 反向触发,**纪律**:完成"留 X 收口"godoc 承诺时同时开 issue 锚定 + grep 关键字 `留 \S+ 收口` 进 milestone closeout checklist;**④ reflection 教训直接转 issue 的工作流二次验证**(承 issue #13 轮 + 本轮 issue #18,24h 内完成教训→issue→修复→闭环 reflection)。**触发场景**:Compile 期不知道运行期会注入哪个后端 / 后端是 plugin 形式时(教训 1 pattern)、修了一个底层机制后 bench 性能反差但白盒探针证机制工作正常时(教训 2 视角)、看到"留 X PR 收口"类 godoc 承诺时(教训 3 转 issue)、写 reflection 时主动标"具备直接转 issue 的成熟度"教训(教训 4 工作流)。
- `memory/reflections/2026-06-17-issue21-short-proto-guard-round.md` — issue #21 short proto 守卫:消除 pineapple p3 反噬 + 修正反思教训 2 估算错(承 [[2026-06-17-issue18-p3-autolift-fix-round]] 教训 2,close pineapple p3 perf 调查闭环)过程教训:**① profile 实测 ≫ 机理推算,复合估算分项任何一条偏 50% 整体结论可能反向**——v4 profile 实证 spike 教训 2「钩税 13% + wasm 反噬 6%」估反了,实际钩税可忽略 + wasm 是唯一主导项(p3 build cpu profile 中钩税路径完全不在 top 200);第三次复发(spike 估错 + issue #18 教训 2 估错 + 本轮)**可升 [[perf-optimization-workflow]] 作 first-class 纪律**;**② 升层触发路径的多源性**——`OnEnter`(host loop)≠ 唯一,`OnBackEdge`(user 脚本内循环回跳)同等重要;教训 2 估错根因就是漏算 CommonRow 形式 user 写的 `for` 循环触发 OnBackEdge 升层这条路径;**③ 守卫位置选择「counter 累积后」vs「累积前」是 profile 诊断完整性边界**——第一次实施把守卫放累积前导致 4 个 profile_test 失败(EntryCount/BackEdge 期望准确),挪到「累积后但 considerPromotion 前」既保 profile 诊断又跳过 perf-suboptimal 决策;**④ testing fixture 与生产路径守卫耦合**——新 sampling 守卫前先 grep 所有 fixture 工厂(makeProto / makeProtoWithCode / promoteProto)看依赖,两条修正路径并用:fixture padding(mock 测试)+ forceAll 显式绕过(e2e 测试)。**触发场景**:用机理估算做 perf 投资决策时(教训 1 实测验证)、评估某 proto 升层情况时(教训 2 列两条触发路径)、加 sampling 钩快路径守卫时(教训 3 守卫位置分层)、新生产路径守卫破坏 testing fixture 时(教训 4 padding/绕过双路径)。
- `memory/reflections/2026-06-29-p4-darwin-arm64-codepage-ci-接入.md` — P4 darwin/arm64 codepage 真实现 + macos-latest CI 接入轮(PR #27 本批 14 commits `278cf12..bfc0ab1`)过程教训:**① 同包 cgo+asm 物理边界**(Go 工具链禁止同包 cgo 文件 + Plan 9 asm 共存,W^X cgo 必须拆 `internal/jit/jitcgo/` 子包 forward 模式,保主库纯 Go 零 cgo)/ **② QEMU 用户模式不真支持 mprotect RWX→RX**(arm64 CI 翻 true 需 macos-latest M1 真机机,linux/arm64 仅 QEMU 不足覆盖)/ **③ 跨 arch 浮点 ULP 容差**(amd64 87-bit ext / arm64 64-bit IEEE-754 在 sin/cos 等超越函数尾位差异,e2e 断言放 ULP 容差非 byte-equal)/ **④ macos-latest perf threshold 偏苛**(GitHub macos runner 共享 CPU 抖动大,perf 类断言要么 skip 要么放宽阈值 ≥3x 余量)/ **⑤ runtime.GOOS DCE 陷阱**(`if runtime.GOOS == "darwin"` 在交叉编 GOOS=linux 时整段 DCE,W^X 平台分支须用 build-tag 文件物理隔离非运行时判断)/ **⑥ codepage-only sanity probe 隔离根因**(真 execute SIGSEGV 时先剥离 emit/relocate 跑「只 mmap+mprotect+ret」最小 probe,把根因从「全链路」收敛到「W^X 系统调用本身」)。**当前状态**:linux/arm64 检查翻 true ✅ + darwin/arm64 codepage 真实现(jitcgo 子包)+ macos-latest M1 接入 CI 安全子集(到达 CI 绿,真 execute SIGSEGV 留 followup PR Mac 物理机调试),darwin/arm64 检查暂 false。promotion 候选:教训 6「codepage-only sanity probe isolate 根因」是 [[prove-the-path-under-test]] guide 正向侧解药节第二实例(首次样本 VS0-e 之后),候选升 guide §正向侧解药节补充,**首次该样本暂留观察**;教训 1「同包 cgo+asm 物理边界」+ 教训 5「runtime.GOOS DCE 陷阱」首次样本暂留。**触发场景**:接入跨 arch CI 时(QEMU vs 真机机覆盖差异)、做 W^X / mmap-RX-RWX 类系统级 JIT 时(同包 cgo+asm 物理边界 + DCE 陷阱)、写 perf 类断言跑共享 CI runner 时(阈值余量)、e2e 真 execute SIGSEGV 时(codepage-only sanity probe 剥离 emit/relocate)看这篇。
- `memory/reflections/2026-06-24-p4-doc-review-round.md` — P4 文档审查轮(v0.2.0-rc5..f24684a,主助理派 4 子代理并行审 8143 行 / 10 文件 / 24 项问题)过程教训:**① 跨文档传递 perf 数字基线丢失 = 隐蔽坐标系错误**(loop 2.95x over P1 与 luajc 4.4x over gopher-lua 被串成 2.95x ≪ 4.4x,实际 7.2x > 4.4x;已扩散至 index.md / startup.md 影响 P4 立项动机叙事)首次样本暂留,候选并入 [[perf-optimization-workflow]] §5「跨文档传递维度」;**② multi-doc-drafting 收口表只查来源齐全性、未查目标侧一致性**(RJ-7 vs RJ-8 在 P2 04 目标处方案对立)首次样本暂留,候选入 [[multi-doc-drafting]]「目标侧一致性二次审查」新纪律;**③ 外部依赖现状(wazero / Go / 平台 API)+ 内部章节号易过期**(wazero internal/engine/compiler 已切 wazevo / wazero darwin/arm64 MAP_JIT 实现不存在 / Go 1.22 已过保 / 子目录扩展期 ~20 处章节引用错位)**已跨过 3 实例阈值**(联 [[2026-06-14-p3-pw7-pw4b-closure-tforloop-round]]「难点过期」+ [[2026-06-16-vs0e-varargs-stack-underflow-round]]「调研先于实现」),强烈建议提升,候选落 [[design-claims-vs-codebase-physics]] §5「时间维度」新章或独立 guide;**④ 工作流确认**:并行 investigator 审查 + 主助理收口写最终报告 = 大文档审查有效模式(multi-doc-drafting 三协议审查侧首次镜像零阻塞),建议作 [[multi-doc-drafting]] guide「适用面延伸到审查侧」实践证据。**触发场景**:大型文档扩展后做质量审查时(并行 investigator + 主助理收口)、大型文档发布前(对外部依赖现状 + 内部章节号做事实层 checklist)、多人 / 多子代理并行起草后做单点收口时(检查目标侧一致性 + 来源齐全性)、跨文档转述 perf 数字时(必带 over baseline)看这篇。
- `memory/reflections/2026-06-30-pr27-f3-3b-darwin-arm64-execute-roundup.md` — PR #28(承 PR #27 F3-#3b)darwin/arm64 真机 execute 闭环轮(3 commits / 4 bug 修复 / 5 小时,base `feat/p4-reworded`):本机 M1 上完成 PR #27 留下的 arm64 真机 BL→mmap 执行调试尾巴,把 `archSupportsSpec`/`archSupportsFrameInline` 五处占位翻 true + macos-latest CI 跑全套。**头条「bypass trampoline 探针 20 行 + 5 分钟把三 hypothesis 收敛到一档」已 promote 进 [[prove-the-path-under-test]] §5**(机制叠加 N 档崩点一样的 SIGSEGV/SIGILL 形式时写 minimal payload 直接调下一层 bypass 当前调试层,本会话 H1 trampoline ABI / H2 PAC / H3 entitlement 三 hypothesis 一次性收敛到 H1);**「sentinel 注释承诺与检查状态解耦」已 promote 进 [[design-claims-vs-codebase-physics]] §5「时间维度」**作家族第 4 形式(`arch_arm64.go::archSseOpForArith` stub 注释「当前检查返 false 本函数不会被调用」在检查翻 true 后变静默 bug,与 PW7 难点过期 / VS0-e 调研先于实现 / P4 doc-review 外部依赖现状过期同源)。其它实例:bug #1 trampoline_arm64.s `STP (R19,R20), 0(RSP)` 覆盖 Go arm64 auto-prologue LR slot @ [SP+0](三相等指纹 `pc=lr=x19`)/ bug #2 ciSegBase byte offset 协议 amd64 §9.20.9 commit-5l 已修过 arm64 PJ8 漏改 / bug #3 Pop 段尾缺 ret 一样的。**amd64↔arm64「同源 bug 漏改」纪律暂留观察**(同会话 2 实例同接入面,模式跨会话稳定性需后续 PJ-N / P5 后端再现验证)。**触发场景**:调试机制叠加 N 档崩点时(bypass 探针根因 isolate)、检查翻 true 同 commit(grep stub sentinel 注释承诺)、多后端镜像接入前(grep 前后端历史 fix commit 逐条对位)、看 PR comment 列 N 个 hypothesis 时(默认探针而非穷举)、跨平台 trampoline ABI 调试时(Go arm64 auto-prologue LR @ [SP+0..8)、FP 由 `STUR X29, [SP, #-8]` 写 user space 外故不占 user 区,user STP 从 [SP+8) 起)看这篇。
- `memory/reflections/2026-06-30-p4-pj10-perop-translator-round.md` — P4 PJ10 通用 per-opcode 翻译器初版交付轮(2026-06-30 单会话 21 commits `a94bcec..HEAD`,~3420 行,80 e2e subtests 全过,`make test-p4` 全绿含官方 Lua 套)过程教训:**① 正确性 floor(语义覆盖 = Go 端回放)vs 性能 ceiling(native amd64 multi-BB CFG emit + label resolver)拆分**——设计稿 §3 原画 native CFG 是「性能前提」非「语义前提」,实际完成选「Go 端回放 + 占位 mmap stub `xor eax,eax; ret` 3 字节」物理路径,把 35/38 opcode 接住(VARARG 设计永不接 + JMP / TEST / LOADBOOL C!=0 需真 CFG 留 followup),语义与 P1 解释器逐字节一致——架构图同一时间点不分层 floor/ceiling 须实现者动手前显式拆,与 [[design-claims-vs-codebase-physics]] §5「时间维度」对偶(空间维度新维度,首次样本暂留观察);**② stop-hook 升压判据「profile / scope-check 区分可达 vs 不可达」**——[[p3-pw10-architectural-ceiling-round]] PW10 是「数字不可达就止损落档」、本轮 PJ10 是「范围可达就真的写完」(两次 stop-hook 升压都被强制不结束,实测 TFORLOOP 是固定 4-op pattern + CLOSURE SubNUps + CLOSE 单点 host.Close + FORPREP/FORLOOP 数值循环 Go 端 dispatch 完全够用,0d7cbd2 + 4e0e863 两个 commit 真补齐——这是与 PW10 升压结果方向相反的对偶轮),首次明确升压方向判据,暂留观察;**③ CLOSURE SubNUps 跳 pseudo 数据字 idiom 第 2 实例复发**——P3 PW7 [[p3-pw7-pw4b-closure-tforloop-round]] 教训 2 + P4 PJ10 peroptranslator 同 idiom 跨翻译器,**已跨过 2-实例阈值**,建议在 PW7 反思教训 2 反引本反思作实证(memory 内反引不升 guide;P5 trace JIT 第 3 实例再升 guide);**④ P2 bridge F2-b 白名单决定 P4 测试可写形式**——pairs/ipairs 不在 F2-b 安全调用白名单 ⟹ `for k,v in pairs(t)` TFORLOOP 测试 proto 在 P2 检查被烧 ReasonUnknownCall 不升层 ⟹ P4 PJ10 永远不被走到,解法 hand-written iterator(`customIter` 经 F2-b 静态分析判定已知调用形式),与 [[issue18-p3-autolift-fix-round]] 教训 1 是 pattern 家族但维度不同(本条空间维度过滤器、那条时间维度 lifecycle),首次样本暂留观察。**promotion 决策**:全 4 条教训 **首次样本暂留观察**,本轮不升 guide(教训 3 CLOSURE SubNUps 跨过 2-实例阈值建议 memory 内反引到 [[p3-pw7-pw4b-closure-tforloop-round]] 教训 2 作实证,P5 trace JIT 第 3 实例再升 guide「字节码 opcode + 后随数据字 idiom 跨翻译器复用 `SubNUps[]` 跳表」)。**触发场景**:照设计稿完成任何新里程碑前(教训 1:先把设计稿主张拆「正确性 floor」+「性能 ceiling」两层,floor 优先把语义一次接住,性能件按 profile 决定何时上)、`/goal` stop-hook 强制不结束时(教训 2:先 profile / scope-check 区分「可达只是还没做」vs「真的不可达需止损」,前者真写、后者止损落档,**绝不**只把 commit message 写「留 followup」推迟到后续里程碑)、给新翻译器(P5 trace JIT / 新后端)写 opcode 发射循环时(教训 3:`Proto.SubNUps[Bx]` 是规范级跳表,CLOSURE 后随 pseudo MOVE/GETUPVAL 数据字 pc 步进经它)、给某 tier 写 e2e 用到 stdlib 函数(pairs/ipairs/string.* 等)前(教训 4:先 grep `f2bSafeFuncs`/`checkF2bSafeCall` 看是否在 P2 bridge F2-b 白名单,不在则 hand-write 等价 lua 形式)、看 PJ10 物理路径分歧(设计稿 §3 native amd64 multi-BB CFG vs 实际 Go 端回放 + 占位 stub)时(这是「正确性 floor 已交付、性能 ceiling 留 followup」的实证样本)看这篇。
- `memory/reflections/2026-06-30-pr28-f3-3c-tri-platform-matrix-ci.md` — PR #28(承 F3-#3b)F3-#3c 三平台矩阵 CI 等同轮(`efcfe5e..5afea11`,5 commits / 5 小时 / 39/39 CI checks + machine APPROVE):把 PR #28 推到「arm64 与 amd64 地位等同」CI 形式——test/fuzz-smoke/conformance/difftest × {amd64, ubuntu-24.04-arm, macos-latest} × {p1, p3, p4} 矩阵化,踩 **5 个独立 macOS 兼容性 paper cut**(brew lua@5.1 撤 / actions/cache symlink 半残 / bash 3.2 `${arr[@]}` set -u / bash 3.2 `mapfile`+`declare -A` / fuzz fail input 丢失)总周期 ~2 小时 = 决策成本 4-6 倍。**头条「bash 3.2 vs 4+ 兼容性 + actions/cache symlink + homebrew 包政策」已 promote 进 [[design-claims-vs-codebase-physics]] §6「空间维度」新章**(跨 OS / shell / runtime 物理环境差异,与 §5「时间维度」并列;含 §6.2 bash 3.2 vs 4+ cheatsheet 三行表 + §6.3 actions/cache symlink tar 语义 + §6.4 grep checklist)。**「跨平台等同决策成本预算 N 倍 paper cut」+「fuzz fail 必须 artifact upload」+「PR comment hand-off 跨平台 bug 通道」三候选首次样本暂留观察**(后续 P5 后端跨 OS / cross-arch fuzz 扩面 / 新 OS 接入再现可升 guide;fuzz artifact upload 是 [[prove-the-path-under-test]] 对偶面「证 input 在 testdata 而非 runner 销毁」,留 hook 不丢)。**触发场景**:做「跨平台 / 跨 N 维度等同」类简单决策时(预算 N 倍 paper cut)、写 bash 脚本要跨 OS 跑时(`grep -nE 'mapfile|declare -A|\$\{.*\[@\]\}'` 是提交前默认动作)、加 `actions/cache` 缓 symlink 类文件时(切「缓源目录 + restore 后重 install」)、加 fuzz CI 时(配 `if: failure() + actions/upload-artifact testdata/fuzz/`)、跨平台 bug 没本机复现路径时(PR comment 列复现路径+artifact 下载+hypothesis 三档)、GHA cross-arch CI 接入前(查 runner image 别默认走 QEMU / self-hosted,`ubuntu-24.04-arm` 已 GA 真机)看这篇。
- `memory/reflections/2026-07-02-p4-beat-p3-opset-round.md` — P4 must-beat-P3 op-set 扩面轮(2026-07-02,分支 `feat/p4-beat-p3`,30 commits `dc9baf0..79bd6dc`)过程教训:承 [[2026-07-01-p4-pj10-native-round]] 把 P4 amd64 native 相对 P3 wasm 的对比面从少数几核推到 25/27 对基准 ≥2%(多在 +10%~+85%),README perf 表整节更新。**七条教训**:**① exit-reason 协议解 mmap+morestack 物理不兼容的「第三态」**——`16bd225` 引入 mmap 段不 call Go,而是把 `(helperCode, a, b, c, pc)` 打包进 `jitCtx.exitArg0` + 状态字置 `ExitInlineHelper` + `RET`,Go 端 `nativeCode.Run` 是 dispatcher 循环解包 → 分派 host 方法 → `RefreshJitCtxAddrs`(arena 可能已 grow)→ 从 `codePage + resumeOff` 重入,把 [[2026-07-01-p4-pj10-native-round]] 教训 1 的「shim call in mmap vs saveGoG Go-side dispatch」二态划分升级为「U 形 exit-reason」第三态,opSupported 从 18 op 扩到覆盖 CALL / UNM / GETUPVAL / SETUPVAL / GETGLOBAL / SETGLOBAL(18-bit Bx 拆两 9-bit payload)/ 多值 RETURN(HelperReturn 终结不重入);首次样本暂留,P5 trace JIT / OSR exit 再撞可升 guide「受限执行环境的 exit-reason dispatch 协议」;**② invariant consumer 若跳过 per-access 复验,对 producer 纪律的要求严于解释器路径**——`cc6557e` 修的最深 bug:`insertNewKey` Brent 式重定位改 key→slot 映射但没 BumpGen,解释器 `icGetTable` per-access 复验 NodeKey 掩盖多年,P4 native GETGLOBAL NodeHit inline 快路径 + P3 wasm `emitGetGlobal` 一样的 gen-only guard 是同一颗定时炸弹,fuzz seed `4b3d10ff` 抓出;**invariant 强度不是由 producer 单方面定义,是由最严格的 consumer 定义**;**首次样本足够强**,建议作 [[design-claims-vs-codebase-physics]] §2「跨层固定 token / 视图段重定位」的**时序对偶**补充(那节管空间性 held-pointer,本条管时序性 gen-guard),或独立立项 guide「invariant contract 强度 = 最严 consumer 定义」;P3 wasm 侧确认修复覆盖 + 若 P5 出现第 3 consumer 即升 guide;**③ 验收门(entry gate)与 emit 质量对性能收益贡献相当,且方向相反容易被误读**——GETGLOBAL ungated 反回归(Transform CallInto 290us→332us,`ff6ac62` revert opSupported 接受 → `7b17b70` 带 NodeHit-IC gate + inline 快路径回来变 +11%,同一 emit 接受策略切换 25 个百分点全景)/ CALL 密度门 `totalOps/callCount>=16`(fib 类 CALL 密集 body 单次 exit round trip ~15-25 解释器 op 单纯亏)/ MinPromotableLener hook(bridge 加 per-backend hook,P4 实测 fixed Run 成本 ~111ns > 解释器 78ns 使 tiny proto 下地板停 10);**接受面 vs emit 面按 op 名合成单张 opSupported 表把两个正交轴强绑**,shape-dependent 净胜的 op 拆「emit 能力(始终有)」+「接受门(shape 判据)」;与 [[2026-07-01-p4-pj10-native-round]] 教训 3「prefer-native 拦截过宽」是同类**入口判据窄化**家族的 op 粒度版,共同基础「实测再定判据」;**④ Fuzz 是唯一能兜底 gen-only 快路径 + inline 类快路径的现实防线**,本轮三种子实证:(a) `f7f0bb1a` x86 SSE NaN 结果 `0xFFF8...` 与 NaN-box tag 空间别名 → arith inline 加 result guard 路由 `host.Arith` canonicalize / (b) `4b3d10ff` insertNewKey Brent relocate 无 gen bump(见教训 2)/ (c) `d9bce2e` LT/LE inline UCOMISD 缺 IsNumber 守卫使 reg operands boolean 时静默比较位模式;共同结构「inline 快路径省完整校验 → 常规 test 覆盖不到 → fuzz shape 混合扫出」,三种子入 corpus `testdata/fuzz/FuzzP4ForceAllPromote/`;**⑤ 共享分析器改进可能是 per-backend 回归**——`45b8b53` dataflow-track stdlib alias(`local sqrt = math.sqrt` 类;funcState.localAliasAsts 记 AST 而非 name,`AnalyzeProtoWithOuter` 加 `outerAliases` 参数,visitor.safeAliases 处理 reassignment/shadowing/scope 失效)让 nbody 过 F2-b 白名单升层,P4 效果 874ms→43.2ms(~20x),**P3 wasm 侧同 shape 反 43.5ms→89.7ms(≈2x,issue #39)**——一个 proto「能升」≠「升了赚」,共享分析器改动后每个后端独立跑一次 all-bench 复测,若 per-backend 分歧优选(a) 收窄共享判据 / (b) 加 per-backend 净胜门(`MinPromotableLener` hook 形式可扩)/ (c) 记 issue 交后续里程碑并在 memory 显式记录 per-backend 分歧数字;**⑥ 共享机器 + 无限制并发 fuzz + pgrep 轮询壳把 load 打爆的过程雷**——共享 80 用户机器上并发跑多个 `go test -fuzz`(默认 24 worker)+ `while pgrep` 轮询壳把 load 打到 11+ 被 kill;规则(已入用户 memory):fuzz `-parallel=4` / bench 串行 `-cpu=1` / 不写轮询壳 / 一次一个重活;**⑦ Bench 方法论三雷**——P3/P4 并跑互扰(须严格串行同 -cpu=1 同硬件时间不重叠)/ `-race` 二进制 profiling(racecall 顶到 top 盖真瓶颈,perf 工作用无 -race build)/ build tag → bench 名分歧(P4 tag 下叫 `GibbousJIT*` 默认 build 叫 `Gibbous*`,-bench regex 不匹配时 `go test` 静默 output `PASS`+ 0 iter 无 error message);perf 会话开始先跑「探针命令」验数字非零 + 名字匹配。**验证**:P4 amd64 vs P3 wasm 25/27 对基准 ≥2%(多在 +10%~+85%),2 对 CallOnly-auto 平手(解释器 vs 解释器噪声,README 显式记录);fuzz 120s / 150s(-parallel=4)全绿,3 新种子入 corpus;V14 luajc 档无回归;V15b heavy P4>P3 未退;README perf 表全表更新(2026-07-02 f736c92,「≥1.5x 加粗」规则一致化 79bd6dc);follow-up 已开 issue #37(arm64 exit-reason port,arm64 shim-free 设计天然适合)/ issue #39(P3 nbody 回归)/ task #57 待办。**promotion 决策**:教训 2「invariant consumer 定义强度」样本足够强建议 [[design-claims-vs-codebase-physics]] §2 时序对偶补充或独立立项,余六条首次样本或已入用户 memory(process-level 不升 guide)暂留观察。**触发场景**:mmap / 受限执行环境 codegen 想调 runtime helper 时(教训 1:U 形 exit-reason)、引入 gen-only / snapshot 类 invariant consumer 前(教训 2:producer 侧列全所有能改语义但不 bump gen 的路径 + fuzz corpus 主动覆盖 + 文档写清 invariant 强度契约)、新 opcode 接入 opSupported 前(教训 3:emit 能力 vs 接受面拆两层,shape-dependent 净胜的 op 带 shape 判据 gate)、写 inline 快路径省完整校验时(教训 4)、改共享分析器 / 通用可升层性判据时(教训 5:per-backend 全 bench 复测)、共享 / 多租机器上跑并发工具前(教训 6:强制降 -parallel/-cpu 到个位数,不写轮询壳)、perf 会话开始时(教训 7:探针命令验数字非零 + 名字匹配,profiling 前查 -race,bench-all wrapper 加输出行数 sanity)看这篇。
- `memory/reflections/2026-07-01-p4-pj10-native-round.md` — P4 PJ10 native emit 轮(2026-07-01,分支 `feat/pj10-native`,`ed2235b..0c9db3a` ~15 commits)过程教训:承 [[2026-06-30-p4-pj10-perop-translator-round]] 的 Go 端回放骨架 + `xor eax,eax; ret` 占位 stub,一次性做到位换成真原生 codegen——CFG builder + 两遍 label resolver + 35 opcode 每 arch 一份 emit(amd64 / arm64)。V15b heavy 三本 P4 native > P3 wasm 达 HeavyArith 2.3x / HeavyRecursion 2.3x / HeavyFloatloop 3.0x(10 iter × 3 samples)。**六条教训**:**① mmap 段内不能调 Go helper——morestack 走不过 unregistered code page,热路径必须 inline,冷路径退 Go 端 dispatch**(Go 1.17+ ABIInternal R14=G / `morestack` prologue 检查 SP vs g.stackguard,进入 mmap 段后 PC 不在 `_func` 表 unwinder 撞死),物理约束决定 opSupported gate 形状:18 op mmap-safe 子集 inline,其余 op saveGoG + shim;首次样本暂留观察,后续 P5 trace JIT 再撞可升 guide「mmap 段内 helper call 的可行性 gate」;**② RK 字段 9-bit 表示层交接边界口径切换,uint8 截断 K 引用低 8 位**——shape-spec 层负责 RK 拆分 native 层需自拆 ⟹ 函数签名沿用 uint8 B/C 会把 C=256 截成 0,`f778ab7` 拓宽到 int;与 [[2026-06-30-p4-pj10-perop-translator-round]] 教训 3「CLOSURE SubNUps 跨翻译器复发」同结构(同一条 bytecode 物理事实每个翻译器独立撞),两者构成「Lua 5.1 spec 物理事实 · 翻译器边界独立撞」家族第 2 实例;**③ prefer-native 拦截过宽立刻压掉 25 个已调优测试 —— 判据必须精确匹配 shape-spec 触达不到的形式**(初版 `4b5abf8` AnalyzeNative 就走 native,PJ3/PJ5/PJ7 shape-spec 测试全红;`0c9db3a` 收窄到「多 BB + ≥4 op live BB」)——与 [[2026-06-16-p3-pw10-architectural-ceiling-round]] 教训 1「profile 才是合同」是**入口侧对偶**(那管止损,本管窄化),共享判据基础「实测再决定形式」;**④ Tail-call gibbous dispatch 首版误重入 enterGibbous 双压帧**——`enterGibbous` 内含 `enterLuaFrame`,与 SetTailcall 已复用父帧组合成双压帧使 base 错位到实参数字上,解法 tail-call 帧上直接跑 `code.Run` inline;**⑤ arm64 vs amd64 分支编码差异要求 arch-aware label fixup**(amd64 rel32 / arm64 B rel26 word-scaled / B.cond rel19),`65cb85f` 加 `fixupKind` enum + `addFixupKind`——多 arch label fixup 从起点带 kind enum 别单 arch 写完再补;**⑥ FORLOOP inline 前向 rel8 手算精算**——`000b6bf` FORLOOP inline 里 jae / jmp 的 rel8 按字节精算,`movsd xmm0,[rbx+disp32]` 是 8 字节不是 9,能用符号 fixup 就用不用 rel8 手算;必须 inline 时每条指令字节数写在注释里 + 每个前向分支目标块字节数独立列。**promotion 决策**:全 6 条教训**首次样本暂留观察**;教训 2 已跨 2-实例阈值(表示层交接 bit-width 独立核对),P5 第 3 实例再升 guide;教训 3 是 [[perf-optimization-workflow]] §7 profile 才是合同的入口侧对偶,首次以 first-class 教训形式出现暂留;教训 1「mmap 段内 shim + Go morestack 不兼容」是 P4/P5 mmap-based codegen 通用洞察,若 P5 trace JIT 再撞可升 guide。**触发场景**:mmap / unregistered code page 里想调 Go helper 时(教训 1)、从上游 IR / spec 层往下游 codegen 迁,签名要吃更细的字段时(教训 2:bit-width 独立核对)、引入新 tier / 快路径 / 优化档,想改 Compile 入口时(教训 3:入口判据 = 新档独占形式集)、helper 组合(enterLuaFrame + SetTailcall + enterGibbous 类)出现帧栈 / 状态双写时(教训 4)、多 arch codegen 起点(教训 5)、短距前向分支想省 rel32 用 rel8 时(教训 6)看这篇。
- `memory/reflections/2026-07-03-issue40-arm64-stopbleed-round.md` — issue #40 arm64 P4 调试轮阶段 1 止血轮(2026-07-02~07-03,PR #47,f921626/7168648/bf65839)过程教训:issue 把 arm64 HeavyArith 慢 ~20x 归因于「PerOpCode head-op replay 跨界路径」,但该路径 build tag 排除、arm64 二进制里根本不存在——原止血计划「收紧 arm64 升层接受面」若直接实施是无效操作(接受面本来就在拒收)。**五条教训**:**① 退化归因前先证「被怪罪的路径」真的存在/被执行**(**[[prove-the-path-under-test]] 诊断侧对偶,已 promote 进该 guide §6**)——build tag 追踪 1 小时内证伪原根因假设;**② force/auto 模式不对称是免费的根因二分器**——emit/接受面机制影响两种模式,只有 force 专属机制(retry window)才致数字不对称,一次 `-benchtime=1x` 探针矩阵把嫌疑收窄到 force 专属路径;**③「停留重试」状态 × 高频触发点 = 意外的每事件全量重算**——forceAll retry window(前轮 `fd055e9`)组合每回边触发 `considerPromotion`,拒收结果不写回致窗口内每回边重跑全量后端分析(`recheckCompilabilityRuntime` 占 22.38% CPU,HeavyArith 1.5 GB/op),修法按「结果真正可能变化的粒度」(同一次 entry)去重;**④ 树遍历器的递归 guard 必须沿递归链传递,不能绑在 walker 实例上**——`walkFuncExpr` 建 sub-visitor 隔离信号时把递归展开 guard 也隔离(应拷贝继承),致递归闭包字面量无限展开触发 Go 1GB 栈上限 fatal(既有 bug,fuzz 抓出,`bf65839` 修复);**⑤ fuzz 的机器/架构多样性本身是覆盖度维度**——同一 corpus 在 Apple M5 Pro 上 9-80s 抓到 amd64 侧 120s/150s 多轮未抓到的既有 bug,新硬件先跑 fuzz smoke 是廉价高产动作。**触发场景**:接手带根因假设的性能 issue 时(教训 1)、写退化修复计划前(教训 1)、给状态机加 stay-and-retry 语义时(教训 3)、写树遍历器 sub-visitor 时(教训 4)、新硬件环境就绪时(教训 5)看这篇。
- `memory/reflections/2026-07-03-issue37-arm64-exit-reason-port-round.md` — issue #37 / #40 阶段 2:P4 native exit-reason 协议 amd64 → arm64 移植轮(2026-07-03,分支 `feat/p4-arm64-exit-reason` 8 commits,darwin/arm64 M5 Pro 单会话)过程教训:按上一轮预写在 issue #37 评论里的 7 步实施顺序执行零返工(`.llmdoc-tmp` 调查报告已丢失但 issue 评论落点清单存活——**重要移植前调查结论要写进持久渠道**,首次样本);**核心教训「移植不是复制——硬件语义差异让每个 op 都要重判」三实例**:(a) arm64 UNM 的 -NaN e2e 探针抓出 canonNaN sign-flip 变 value.Nil 位模式的静默错果,**回查发现 amd64 emitUNM 一样的既有 bug 双 arch 同修**(移植轮给旧 arch 挖 bug,与 stopbleed 教训 5「fuzz 机器多样性」邻接但机制不同:语义对照而非随机探索);(b) 旧 arm64 scaffold 的有符号整数比较条件码(GE/LT/GT/LE)在 FCMPE unordered(NaN)时错判,换 FP 安全族(MI/PL/LS/HI)后 NaN 无额外分支正确;(c) arm64 不需要 arith NaN 结果 guard(AArch64 default NaN 为正 + 传播保留输入)而 amd64 SSE 需要——同一 guard 两 arch 一必需一多余;**arch 差异也是简化机会**(FCMPE unordered ⇒ Z=0 单 B.NE 替代 amd64 jne+jp 双跳 / 槽地址活在 X3 免 SETTABLE 重算 idx);**e2e 探针载体要避开无关 op 噪声**(GETTABLE inline-hit 探针第一版被 kernel 里 GETUPVAL 的 exit-reason 往返淹掉 dispatched=1192,表改参数传入后干净——[[prove-the-path-under-test]]「探针载体设计」维度候选);过程教训:`git checkout` 整文件清调试代码误回退同文件未提交工作(~200 行重写,应精确反向编辑或先 stash);每步独立 commit + 每步 fuzz 节奏良好(stopbleed 轮用户反馈的直接应用);HelperCompareSlow 首次出现 dispatcher 向段内回传数据的双向通道形式(packed bit0 经 exitArg0 回传 + 段内 resume 块分支,供 arm64 后续 SELF/LEN/CONCAT 参考)。**触发场景**:做跨 arch 移植前(先把调查结论写 issue 评论 + 逐 op 重判语义勿机械镜像)、写快路径命中计数断言时(载体只留被测 op 可产生 dispatch)、清理临时调试代码时(勿 `git checkout` 整文件)看这篇。
- `memory/reflections/2026-07-03-issue45-issue39-round.md` — issue #45(P4 amd64 exit-reason 收尾)+ issue #39(P3 helper 密度收益门)单会话双 PR 轮(2026-07-03,PR #53 + PR #55,均 39/39 CI 全绿 + bot APPROVE rebase merge):PR #53 把 amd64 emit_ops 里最后的段内 shim 调用(EQ / LT/LE 非数字回退 / arith 慢路径 / LEN / CONCAT / SELF / TAILCALL / CLOSURE / CLOSE / TFORLOOP)全部迁 exit-reason 协议 + **删掉 `nativeCode.Run` 的 defer recover() 兜底**(教训 4「删兜底本身是主张的测试」:shim 面清零后 recover 若仍在则「主张未验证」永远悬着,删掉跑全量测试 + fuzz 保持绿即证明) + LEN/MOD/POW 进 opSupported 后 90s fuzz **立刻**抓到 master 上潜伏的 PerOpCode 回放乱序老 bug(seed `21c645c46a1268c6`,`function f()return{0}end f(f())` → P4 报 `SETLIST: not a table` 而 P1 正常;根因 PerOpCode「副作用先跑、head op 后物化」隐含「head 写与副作用读互不依赖」假设,NEWTABLE 进 head-op 集合就破了,三种形式:NEWTABLE head + SETLIST 报错 / LOADK head + SETGLOBAL 或 SETTABLE 静默存旧值;修法 `AnalyzeShape` 预计算 lastReplayPC + 任何 `pc < lastReplayPC` 的 head op **降级**为有序副作用 + `slotKindReg` 读回,第一版全拒误杀 `TestPJ10_SetList` 教训:「先扫全 hazard 面再选拒绝 vs 降级档,拒绝的成本远高于降级的实现成本」);PR #55 给 bridge 加 `PromotionGater { WorthPromoting(proto) bool }` 可选接口 + wasm Compiler 实现 op 构成密度地板 `total/helperBound>=7`(阈值靠白盒 op 构成统计先行 5 fib 漏网 → 7 全部回归内核被拒,教训 5「阈值靠白盒统计 + 实测迭代不要一次定死」),仅 auto 模式生效 forceAll 绕过保差分测试覆盖,拒绝 → TierStuck 吸收;nbody auto 89.7→43.2ms 与 P1 解释器同等、fib 24.9→10.9、binary-trees 104→38.4、spectral-norm 40→20.7 全面回收,heavy 三本 P3 赢面不变;**五教训**:教训 1「接受面/硬件/参数任一维度变化 = 新一轮独立的 fuzz 探索」**已跨提升阈值(第三实例)** → **promote 进 [[prove-the-path-under-test]] §5「fuzz 探索空间维度动了就重探」**(与 [[2026-07-02-p4-beat-p3-opset-round]] 教训 4 + [[2026-07-03-issue40-arm64-stopbleed-round]] 教训 5 三实例)/ 教训 2「后端接受面能力 vs 收益分层」**已跨提升阈值(第三实例)** → **promote 立新 guide [[backend-capability-vs-profitability]]**(CALL 密度门 + `MinPromotableLener` + `PromotionGater` 同结构 hook 家族)/ 教训 3「推迟执行模型必须审计执行顺序 hazard」首次样本暂留观察(与 [[design-claims-vs-codebase-physics]] §2 空间维度构成时间维度对偶,不合并)/ 教训 4「删兜底本身是主张的测试」首次样本暂留 / 教训 5「阈值靠白盒统计 + 实测迭代不要一次定死」首次样本暂留(已入 [[backend-capability-vs-profitability]] §4 工作流)。**触发场景**:接受面 / 硬件 / 参数变动后要不要立刻跑 fuzz smoke 时(教训 1)、某后端要拒收某类 proto 时(教训 2:先分「编错」还是「编了净亏」)、给推迟执行模型 / lazy evaluation 加新 op 到「延后」集合前(教训 3:先列 hazard 表格 + 用最小探针家族扫全实际命中面 + 拒绝 vs 降级)、某 `defer recover()` / fallback 上游触发条件被认为已消除时(教训 4:删掉跑测试 + fuzz 绿灯即主张成立)、选性能相关数值阈值时(教训 5:先跑白盒统计给分布再从分布选阈值再实测边界样本)。
- `memory/reflections/2026-07-06-issue50-emit-call-inline-round.md` — issue #50 Spike 5(PJ10 native 段到段 CALL 分派,让 call 密集内核反超 gopher-lua)amd64 收口轮(2026-07-06,分支 `feat/issue50-emit-call-inline`):做四件事消掉每次 gibbous→gibbous 调用的 exit-reason 往返——段内 GETUPVAL inline(`emitGETUPVALInline`)+ 双语义 RETURN 扩展到 MultiReturn proto(`emitReturnDualSemantics`)+ `ProtoSeg2SegEligible` 把 seg2seg 被调方接受面从 never-exits 扩到 arith/compare(deopt 守卫)+ GETUPVAL(inline)+ 嵌套 CALL + CALL 密度门对 seg2seg 合格 proto 放宽。实测 fib 10.5x / fannkuch 6.9x / binary-trees 1.35x over gopher-lua(同机 `-benchtime=2s -count=3` 中位数),amd64 全部正确性门全绿(difftest-p4 / crescent / conformance-p4 / peroptranslator / `-race` / 20k 随机差分脚本);arm64 因本地无执行环境(无 qemu-aarch64)+ 用户不建 PR 不触发 CI arm64 矩阵,推迟到 issue #61。**五教训**:教训 1「多 RETURN proto 静默破坏 seg2seg,症状是错值不是崩溃」(seg2seg 测试语料须含 multi-BB / 多 RETURN / 递归被调方,单 BB never-exits 叶子对多 RETURN 拆帧路径是结构盲区)是 [[prove-the-path-under-test]] 家族「绿色 ≠ 在测你以为在测的」又一形式,具体维度首次出现暂留观察 / 教训 2「eligibility predicate 调 analyzer、analyzer 回调 predicate = 无限递归」(拆纯 op 检查 `seg2segOpsEligible` 与合成 predicate)首次样本暂留 / 教训 3「open-upvalue owner 解析不能通用 inline——owner 在 Go 侧 `st.uvOwner` map 不在 upval 对象内,仅单线程无协程可 inline,用 `inlineUpvalSafe` 位门控」首次样本暂留,与 [[design-claims-vs-codebase-physics]] §2 邻接但轴不同不合并 / 教训 4「deopt 重跑幂等前提:被调方不写参数寄存器(`dest A >= NumParams` 门控)+ 无外部副作用」首次样本暂留,若 P4 OSR exit / P5 trace JIT 再遇同族可升 guide / 教训 5「arm64 验证是物理 + 用户决策阻塞而非工作量」已入用户 memory「真『不可达』只两类」process-level 不升 guide 作实证记录。**触发场景**:给 seg2seg / 跨段直调机制写测试语料时(教训 1:含 multi-BB / 多 RETURN / 递归被调方)、写「eligibility predicate 调 analyzer」类判据时(教训 2)、给依赖 Go 侧归属信息的操作做 inline 快路径时(教训 3:按运行时形式门控)、给「投机执行 + 失败 deopt-redo 整段」机制定接受面时(教训 4:显式含不写重放输入 + 无副作用两条幂等前提)、某平台本地无执行环境且触发其 CI 验证的前置动作被用户否决时(教训 5:物理 + 用户决策的合法推迟)看这篇。
- `memory/reflections/2026-07-07-issue67-auto-mode-coverage-round.md` — issue #67 auto-mode 覆盖轮(PR #75)过程教训:生产跑 auto promotion,但仓库每套 P3/P4 测试长期只经 `SetForceAllPromote(true)` 驱动,auto-only 决策链(阈值中途越线 / `recheckCompilabilityRuntime` 自然路径 / `PromotionGater` / 地板 + `FloorExempter`)完全无网(issue #67 正是此类 bug);未强制的测试也测不到——脚本永远够不到生产阈值(entry 200 / back edge 1000),auto 退化成纯解释器。新增 `Bridge.SetHotThresholds(entry, backEdge)`(0=保持不变,与 `SetForceAllPromote` 同纪律的 testing-only 入口,只改「何时」触发不改「决策什么」)+ 四类新覆盖(`test/difftest/auto_test.go` / `test/conformance/conformance_auto_test.go` / `FuzzAutoPromote` / nightly auto-mode leg)。**两条教训**:① 「未强制测试 ≠ auto 覆盖」——`PromotionCount>0` 兜底断言是解药,且开发中当场应验(P3 首版语料因 `WorthPromoting` 密度门拒收全部短核而挂,逼语料换长纯算术核),**已 promote 为 [[prove-the-path-under-test]] 家族第 13 个独立实例**;② 有状态差分 harness 的 run-for-run 基线——`FuzzAutoPromote` 首次 CI 跑抓到 harness bug(seed `861f54880d2009d5`,留作回归种子):脚本改写全局变量时同一 State 后续 Run 合法产生不同行为,但 harness 只跑一次基线却跑两次 auto State,把跨 run 状态漂移误判成 tier 分歧;修法基线与被测逐 run 同步对比,首次样本暂留观察。**触发场景**:给某个自然触发路径(auto 升层 / 自然热度)写测试套时(教训 1)、写「同一个有状态对象反复调用 + 独立基线对照」的差分 harness 时(教训 2)。
- `memory/reflections/2026-07-08-issue77-math-intrinsics-round.md` — issue #77 math.* intrinsic emission 轮(PR #87,分支 `feat/issue77-math-intrinsics`,双 arch + CI 全绿 + bot 两轮 APPROVE)过程教训:给 P4 native JIT 加 `math.*`(sqrt/floor/ceil/abs/max/min)intrinsic emission,CALL 站点 IC 观察到被调是已知纯数值 host closure 时段内直接发射硬件指令(amd64 SQRTSD/ROUNDSD/sign-clear/UCOMISD-select;arm64 FSQRT/FRINTM/FRINTP/FABS/FCMP+FCSEL),不再 exit-reason 往返到 Go host closure;是 issue #67 NodeHit inline 后 n-body 剩余 dispatch 几乎全是 sqrt 的直接后续。**四条教训**:**① 给热路径加新特化分支时,把新检查挂在「不适用它的输入本就会走到的既有 guard-miss 慢路径」上,换热路径零开销**——math intrinsic 被调方是 host closure,`protoID` 字段是 `hostFnID` 而非 Lua `protoID`,必然过不了普通快路径的 protoID guard、直接落到 guard-miss 慢路径,把 intrinsic 检查挂在那条慢路径上,fib 等热 Lua 调用走 seg2seg 完全不碰 intrinsic guard(零开销),fib/spectral/fannkuch 无回归实测证实;首次样本暂留观察(与 [[perf-optimization-workflow]] 的流程纪律不同层,是 codegen 结构手法),等第二个不同形状实例确认是通用手法而非本例 protoID guard 布局巧合。**② 数值 intrinsic 的 byte-equal 依据在值表示层的 NaN 规范化,不在指令语义**——`value.NumberValue` 把任何 NaN 规范成 `canonNaN`(值世界唯一 NaN),SSE/NEON 对 canonNaN 要么原样传播(结果 `< qNanBoxBase` 直接存)要么产生负 indefinite(被 result-NaN guard 抓走路由 host),承 arith inline 先例(fuzz bug `f7f0bb1a`);暂留观察但接近阈值,是 [[design-premises]] 第四组「NaN-boxing 值表示承诺」在 JIT 段内浮点快路径上的正确性对偶落点,第三个段内浮点快路径撞同一 NaN-box tag 区时升为显式条款。**③ 机制只在 proto 升 native 后生效 → 测试脚本必须真能升层**——直接 `math.sqrt(x)` 过不了 F2-b unknown-call(proto 不升 native、intrinsic 路径永远到不了),须用 `local sqrt = math.sqrt` 别名(analyzer stdlib alias 追踪认得),是 [[prove-the-path-under-test]] 「未升层测试静默测了解释器」家族又一实例——不只「auto 阈值没到」会静默退化,「调用形式过不了 F2-b 静态门」也会,两者都靠 `PromotionCount>0` 兜住(§2(b) 解药);memory 内反引 [[2026-07-07-issue67-auto-mode-coverage-round]],不新增 guide 正文小节。**④(过程)已知不该提交的 untracked 产物,注意到的当下就加 .gitignore**——`git add -A` 把 pre-push hook 产物 `.rebasechk` 误扫进提交被 bot 抓到,之前明明注意到它是残留还特意没 commit,下一次 `git add -A` 又带进去;已 `git rm` + 加 `.gitignore`(commit `0e78ba6`);首次样本暂留观察。**触发场景**:给某快路径加新特化分支时(教训 1:先问新分支的输入在既有 guard 序列里天然落到哪个 miss 分支,把新检查寄生到那个已经要走慢路径的点)、给 JIT 加产生浮点结果的快路径前(教训 2:先查值表示层怎么规范化 NaN/特殊值,正确性依据在那里不在指令语义)、给只在「proto 升 native 后」才生效的机制写测试时(教训 3:先确认脚本真能升层 `PromotionCount>0`,`math.*` 类须用 `local` 别名过 F2-b)、注意到不该提交的 untracked 产物时(教训 4:当下就加 `.gitignore`,别只在脑里记着不 add)看这篇。
- `memory/reflections/2026-07-08-issue67-amd64-nodehit-crossrun-round.md` — issue #67 amd64 NodeHit 跨 Run inline 补齐轮(PR #82,分支 `feat/issue67-amd64-nodehit`)过程教训:把 arm64 上已修正的 identity-free NodeHit inline(hmask 边界 + nodeRef guard)移植到 amd64,推翻 PR #74「amd64 NodeHit inline 实测反而 ~3% 回归、故 arm64-only」的历史结论——那 ~3% 不是 inline 的成本,而是一道烤进段里的 TableRef 身份 guard 跨 Run 100% 落空的白费开销:n-body 的 `bodies[i]` 这类局部表每个 Run 都在新 arena 偏移重建,promotion 只烤一次快照,身份 guard 跨 Run 必然不通过,段里每次都发射 guard、每次都不通过、每次都照样 exit-reason 往返到 host,inline 快路径跨 Run 从没真的生效过;之前看到的「inline 命中 17 万次」是单个 Run 内的假信号。**度量教训(头条)**:任何投机 / inline 快路径的收益,度量单位必须是**跨 Run(稳态)的 exit-reason dispatch 计数**,不是单个 Run 内的命中数——选错度量单位会把一个从没生效的优化误判成「生效但太贵」,再叠一个碰巧同向的噪声(~3%),就能推出完全错误的架构结论(arm64-only)。修正 commit 6a10721(原在 arm64):身份 guard 对正确性并不必要——一个 node 自己的 key 字段就能唯一标识这条 entry,换成两道与表地址无关的 guard(hmask 边界 `Index <= hmask` + `nodeRef != 0`),同 shape 的表跨 Run 重建也能真命中。本轮把修正后的 guard 移植到 amd64,共用 `emitTableNodeHitPrelude`;实测 amd64 n-body auto 43.5ms→7.0ms(~6.2×),跨 Run exit-reason dispatch 875k→50k,fib / binary-trees / spectral-norm / fannkuch 无回归;arm64 M5 Pro n-body auto 0.98×→5.7×。新增 `TestPJ10_TableNodeHit_CrossRunInline`(断言每 Run dispatch < 500,身份 guard 时代 ~18000),补上 arm64 那一轮缺的跨 Run prove-the-path。**头条已 promote 为 [[prove-the-path-under-test]] §8「度量单位/时间窗口」第 14 个独立实例**(度量侧新维度:单 Run 命中数 vs 跨 Run 稳态 dispatch)。**触发场景**:读一个投机 / inline 快路径的收益数字时(度量单位必须是跨 Run 稳态 dispatch,不是单 Run 命中数;看到「命中 N 次」漂亮但整体数字反而回归,默认怀疑「机制被触发过但跨 Run 稳态从没真生效」)、给 IC / inline 快路径写「把编译期身份快照烤进段」的 guard 前(问「这个身份会不会被跨 Run 重建打穿」,若被测对象每 Run 在新 arena 偏移重建就换成与地址无关的 key/shape guard)、遇到一个历史 perf 裁决(某优化「实测反而回归、故某平台 only / 故放弃」)时(先复查它当年的度量单位是不是单 Run / 单窗口,用跨 Run 稳态计数重测再决定要不要推翻)看这篇。
- `memory/reflections/2026-07-08-pr83-forprep-stackgrow-fallout-round.md` — PR #83(fixes #78/#80/#81)FORPREP + native 调用栈溢出连锁修复轮:承 issue #67 auto-mode 覆盖轮(PR #75),`FuzzAutoPromote` 上线数天内抓到两个此前无测试网的 P4 native-tier miscompile。**#78** 两 arch 共有的 FORPREP 缺 `IsNumber` 数字守卫(nil limit 的 NaN-box 使 FORLOOP 静默零迭代退出,不报 PUC 5.1 的 "'for' limit must be a number" 错;修法三槽守卫 + 新 `HelperForPrep` exit-reason 退 `host.ForPrep` 走 PUC coercion-then-error);**#80** 两个叠加 bug——(a) `nativeCode.Run` 把入口捕获的 `base`(arena 字节偏移)当固定 token 传给每次 `RefreshJitCtxAddrs`,但 host 调用 growStack 会重定位值栈段留悬垂偏移(修法从活线程状态重算 `vsBase`)/ (b) seg2seg 段内直调从不进 Go 侧 `ensureStack`,深递归溢出初始栈段写进邻居 arena 对象(修法新增 `jitCtx.valueStackEnd` + 两 arch fast-body 边界守卫);**#81** nightly workflow `eval` 剥掉 `-tags` 引号的纯 CI 配置 bug。**三条教训**:① fuzz 探索空间维度重置不止「扩接受面」触发,「新增此前从未被驱动过的执行模式(auto vs force-all)」同样触发——确认 [[prove-the-path-under-test]] §5 既有断言第四次验证,不新增分支;② **[[design-claims-vs-codebase-physics]] §2 第二独立实例**:#80(a)与 P3 PW6 的 `$base` UAF 是同一条物理事实(arena 段可被 grow 重定位)在两个独立加速层(P3 wasm trampoline / P4 native dispatcher)里各自被撞中,判据须在新子系统里重新逐条核对,不能假设「P3 已修 P4 就免疫」——**已 promote 补入该 guide §2**;③ 段内快路径必须显式复刻 host 路径隐式维护的每条不变式(host `enterLuaFrame` 隐式 `ensureStack`,seg2seg 段内直调绕过 host 通道也绕过了它维护的保障),与 [[backend-capability-vs-profitability]] 的「host 通道固定成本假设不适用段内通道」是同一二分轴的另一面,首次样本暂留观察。**触发场景**:新增/扩展任何加速层且存在「入口捕获 arena/栈偏移当固定 token」模式时(教训 2)、给绕过 host 通道的快路径设计接受面/守卫时(教训 3)、给自然触发路径新上线测试网时(教训 1,预期短期内抓到既有 bug 是正常现象)。
- `memory/reflections/2026-07-08-pr86-deep-recursion-nosplit-round.md` — PR #86(fixes #85,files #89)深递归双 bug 连锁修复轮:**#85** 深升层递归报假 "C stack overflow"——解释器 Lua→Lua 递归平坦(单 executeFrom 循环,仅受 `maxLuaCallDepth`=20000 约束),P4 路径超出 seg2seg 深度上限后每层递归都是真 Go 重入链(`Run → dispatcher → ExecutePlainCallInlineFrame → enterGibbous → Run …`),每层 `nCcalls++`(上限 200),深度 1000 合法递归被误报;修法为 `gibbousReentryCCallCap = maxCCallDepth/2` 软水位线(frame.go),四个 gibbous 派发入口(doCall 升层分支 / 两个 zero-cross 分支 / executeFrom TAILCALL 派发)超水位回落解释器平坦执行,深度约束回到 20000 与 P1 逐字节一致。**#89**(存量 bug,被 #85 修复暴露成 PR 自己 CI 秒撞的 GC "found pointer to free object"):seg2seg 段内直调每层 `sub sp` 发生在 morestack 不可触发的 NOSPLIT 窗口内且对 linker nosplit 记账不可见,Go 栈守卫只保留 `StackNosplitBase = 800` 字节,旧 `segToSegDepthCap = 128`(~4KB)在 SP 贴近守卫时穿透栈分配踩坏相邻堆对象——旧注释「~4KB comfortably within the stack」对照错了物理量(goroutine 栈大小 vs NOSPLIT 允许量);修法 cap 降 16(worst case 608B < 800B,实测边界 16 从不崩 / 64、128 必崩),提回需接 05 §3.4 设计过未接线的自管 spill 栈 → issue #89。**三条教训**:① 修复本身是 fuzz 维度重置器——一个 bug 修复扩大可达状态空间会立刻暴露下个存量 bug,同 PR fuzz 撞新 crasher 先 master 复现区分存量 vs 新引入,不反射性回滚([[prove-the-path-under-test]] §5 第五次验证 + 新触发方式);② 栈预算声明必须对照正确物理量——NOSPLIT 上下文预算是 `StackNosplitBase`(800B)不是栈大小,mmap 段内 SP 移动 linker 看不见须人工记账([[design-claims-vs-codebase-physics]] 候选新实例);③ 依赖运行时内存布局的 crash 最小化时保持 harness 形状(简化 repro 不崩、原形交错 State 崩)+ `SetGCPercent(1)` 确定性化 + 二分与解析算术交叉验证。
- `memory/reflections/2026-07-08-pr95-spill-stack-fuel-round.md` — PR #95(fixes #89)spill 栈接线 + 燃料计费轮:amd64 侧接线 05 §3.4 自管 spill 栈(trampoline 切 SP 到 64 KiB Go 堆 buffer,进段前读 codeAddr 防 FP 相对寻址踩垃圾、恢复不碰 RAX/R0),`segToSegDepthCap` 16→128;darwin/arm64 M5 Pro 真机接力四项验证全绿。**接力中 FuzzAutoPromote 12 秒撞出 #85→#89 链第三环 bug**:step budget 无异步生产者(计费只在 `st.preempt()` 解释器 call/回边点),seg2seg 段内直调全绕过,cap=128 后深度 ≤128 子树整棵段内跑(~φ^128 次调用零计费),`fib(5510)`+budget 永久挂死;修法 `JITContext.segCallFuel` 燃料计数(段内派发 dec+jz、归零回退 host 路径恢复计费点;有 budget/context 灌 4096 否则 1<<31,无预算负载零感知,消耗燃料重灌时记入 `stepUsed` 同口径计费)。**三条教训**:① 解除 workaround/限制时列出它顺带提供的全部隐式保障(cap=16 碰巧兜底了计费频率),逐条确认正式机制覆盖——「修复即维度重置」三连环([[prove-the-path-under-test]] §5);② 异步信号(preemptFlag)vs 同步计费(stepUsed)是两种模型,段内感知同步计费须用燃料/配额预支而非 flag 检查;③ 真机接力 fuzz 摇测不可省——交叉编译 + CI 矩阵全绿仍 12 秒撞出真 bug(QEMU 不真模拟 mmap 执行、CI fuzz 时长远短于本地)。
- `memory/reflections/2026-07-09-issue52-close-round.md` — issue #52 收口轮(PR #99):补齐 `opSupported` 最后四个 op(TAILCALL/TFORLOOP/CLOSURE/CLOSE,双架构 exit-reason,协议形态各镜像既有先例——HelperReturn 终止 run / HelperCompareSlow verdict 回段 / P3 伪指令 skip / 普通往返),白名单只剩 VARARG 永久门。**关键教训**:P4 验收 e2e 抓到 clean master 上就存在、P1/P3/P4 三层共享的 `doTailCall` 丢多返回值存量 bug(传父帧定长 nresults 而非 PUC 的 LUA_MULTRET,`return unpack(t)` 丢第三个值)——「给此前无 native 覆盖的 op 写验收测试 = 新一格探索维度,抓到的往往是被解释器宽容路径掩盖的存量 bug」([[prove-the-path-under-test]] §5 家族再确认);操作纪律:新路径测试失败先 oracle 定期望、再 clean master(git stash)定归属,两步做完才决定「修改动」还是「独立 commit 修存量 + conformance 锁三层」;e2e 手算期望值必先过 oracle(本轮手算 12,oracle 说 27)。另记 GHA hosted runner 池未接单的 CI 基础设施噪声识别特征(conclusion=cancelled + steps 空 + "not acquired by Runner" annotation,处置 rerun 即可)。
- `memory/reflections/2026-07-09-issue91-94-p3-audit-round.md` — P3 摸底四连修复轮(PR #100/#101):一场 P3 auto 模式生产路径 probe(挂 bridge Logger 逐 proto 看升层决策 + cpuprofile + 控制变量 kernel 扫描)一次产 4 个带实证 issue,根因是 P4 上线后 P3 auto 生产路径长期缺 probe——「没有观察就没有发现」。**#91**:relooper `computeScopes` 只处理单向 improper overlap,漏对称方向,顶层 if/else diamond 永远升不了 P3(连 force-all 也不行),`normalizeScopes` 定点迭代修复 + 白盒 `TestP3_TopLevelDiamondPromotes` 断言 `PromotionCount>0`(byte-equal 分不清升层与静默 CFAIL 回退,bug 藏这么久正因此);**#92**:`WorthPromoting` 缺 back edge 维度,直线小体升层每次付 wasm 边界往返却无循环摊薄,加 `straightLineMinCodeLen=32` 拒(仅 auto);**#93**:README P3 列分母错配(kernel×50 vs 裸顶层×1)低估 ~50 倍;**#94**:`Bridge.OnEnter` 每帧查 map,加 `OnEnterID`/`OnBackEdgeID` 走 ProtoID 索引 slice。**核心教训**:教训 1 能力修复 ≠ 收益改变(fannkuch 能编但不赚,正面确认 [[backend-capability-vs-profitability]] 分层)、教训 2 互补 PR 合入顺序(收益门先合防回归窗口)、教训 3 fixture 随升层判据多维化演进(第 2 实例连 issue #21)——**三条均 promote 进 [[backend-capability-vs-profitability]]**(§3 fannkuch 实例 + §3(e) back edge 维度 + §4 互补 PR 合入顺序 + §6 fixture 多维化),#91 白盒 `PromotionCount>0` 补进 [[prove-the-path-under-test]] §2(b);教训 4 跨机器 bench 接力用 PR 评论(第 2 实例连 issue #89,接近阈值暂留观察)、教训 5 集中 probe 产批量 issue(暂留观察)。**触发场景**:某后端 auto 收益回归 / 能力修复与收益门分 PR 并行定合入顺序 / 改升层判据后热度类 fixture 连锁失败 / 给某条长期没人看的生产路径体检时看这篇。
- `memory/reflections/2026-07-09-pr101-session-side-findings.md` — PR #101 会话侧补充(与上篇同轮互补,arm64 侧):**① issue #102 的发现路径**——PR #101 无关的 fuzz-smoke 失败经 clean master 归属确认为存量 bug(P4 段内 FORLOOP 回边不计费 step budget:#89 的 segCallFuel 只计费了 seg2seg CALL 派发,回边这半没做;#77 intrinsic 内联消掉循环体最后一次跨 Go 机会后窗口密封,`SetStepBudget` 下解释器 40ms 报错 / P4 force 9s 跑完 2.77 亿次迭代),处置 = 开 #102 + PR 评论说明 + rerun,**慢 seed 故意不入 corpus**(会让每轮 CI 决定性重撞 hang 检测器);教训:给绕过 host 的快路径接计费时须**枚举全部段内控制流形态**(CALL 派发/循环回边/TFORLOOP 继续臂),不是只修触发用例那条——#89→#102 是「修复即维度重置」链的又一环。**② 多机同分支 push 事故**(工作流失败记录):未经指令 commit+push + pre-push hook 内层 force 语义覆盖了队友刚推的 commit;纪律 = 指令到哪步停哪步、协作分支 push 前必 fetch、push 被拒绝不无脑重试先看拒因。**触发场景**:CI fuzz 撞慢 seed 要不要入 corpus / 给段内快路径接抢占计费 / 多机接力分支上要 push 时看这篇。

---

## 设计文档集路由(llmdoc 之外的源文档)

- **入口**:`docs/design/architecture.md` §0 是文档集地图(包布局/组件图/tier 映射);`docs/design/p1-interpreter/00-overview.md` 是 P1 施工计划与**跨文档定稿决策速查**(§4)。
- **战略层**:`docs/design/roadmap.md`(引用时用 `docs/design/roadmap.md` (§N))。
- **验收口径**:`docs/design/p1-interpreter/12-testing-difftest.md` §10 是 26 条验收口径总表(所有「待定口径」的收口点;评审轮新增第 26 条 ColInt64,勿引用旧版 25 条说法)。
- **实现现状**:`docs/design/p1-interpreter/implementation-progress.md`——P1 里程碑进度、验收数字、收尾轮完成表与**设计文档对账表**(原「已知简化」清单已在收尾轮全部完成;值栈/CallInfo arena 化等 P3 迁移留口见对账表)。**问「实现到哪了、实现形式与设计文档的差异」看这篇。**
- **P2 文档集**:`docs/design/p2-bridge/`(子目录 8 文件)——从 `00-overview.md` 进入(文档地图 §0、P-Bridge 里程碑 §4、跨文档定稿决策速查 §6);PB0-PB7 + 后续优化轮 #1-#4 全过线,实施现状对账见 `p2-bridge/implementation-progress.md`。
- **P3 文档集**:`docs/design/p3-wasm-tier/`(子目录 10 文件,2026-06-13 从单文件 380 行扩展)——从 `00-overview.md` 进入(文档地图 §0、P-Wasm 里程碑 §4、跨文档定稿决策速查 §6、四项税 wazero 外包 §8)。子文档分工:`01-spike-gate`(开工检查 <150ns)/ `02-translation`(全 opcode 翻译表 + WAT)/ `03-memory-model`(arena 收养 wazero memory)/ `04-trampoline`(跨层互调 + CallInfo bit50)/ `05-safepoint-gc`(三类 safepoint 收口 P1 06 §12)/ `06-ic-feedback-consume`(非投机消费)/ `07-coroutine-thread-rule`(线程级 tier 规则)/ `08-testing-strategy`(V1-V18 验收)。
- **P4 文档集**:`docs/design/p4-method-jit/`(子目录 10 文件 ~8200 行,2026-06-24 从单文件 360 行扩展,与 P2/P3 同档详细深度)——从 `00-overview.md` 进入(文档地图 §0、P-JIT 里程碑 §4、跨文档定稿决策速查 §6、四项税收回 wazero 外包 §8、不变式 §9、风险 §10)。子文档分工:`01-launch-judgment`(立项检查 + luajc 档锚点 + 两条进入路径)/ `02-template-direction`(方向裁决 = JSC Baseline 风格 per-function 模板编译,候选谱系否决)/ `03-speculation-ic`(IC 反馈消费 + f64 快路径 + guard 显式比较硬约束 + P2 三态不变 + P4 内部 p4SpecState 子状态机叠加,方案 A)/ `04-osr-deopt`(OSR exit 协议 + 物化 = memmove + 栈槽真相不变式 + 再训练防 deopt 风暴)/ `05-system-pipeline`(四项税自付 + W^X / icache / trampoline 四件套 + JIT 世界边界 + arena base 重载)/ `06-backends`(amd64 + arm64 双后端共享骨架 + per-arch 发射器,寄存器约定,模板族分级,双架构 CI 双跑)/ `07-p3-retirement`(P3 去留决策框架,缺省退役)/ `08-testing-strategy`(luajc 档验收 + V1-V22 P4 增项 + deopt 注入 + 双架构差分套)。**当前状态:多 PJ 已完成**(2026-06-28):PJ0 包骨架 + bridge 注入 / PJ1 spike 检查 + amd64 工程组件 / PJ2 投机三形式(reg-reg/reg-K/chain-KK)/ PJ3 FORLOOP 字节级 inline 实测 7.15-25.41x over gopher-lua(超 luajc 档 4.4x)/ PJ4 表 IC 完整六路径 + 严密 IsTable guard / **PJ5 CALL void 220 子 + TAILCALL 102 子 + SELF inline 完整 0..7 参 + 嵌套 + 错误冒泡 + V18 -race + p4SpecState 子状态机骨架**(2026-06-28 单会话 16 commits 累计交付)/ PJ7 接上 ~25 类形式 byte-equal / PJ8 arm64 字节级模板矩阵完整 + Compile 端接上 / **PJ10 通用 per-opcode 翻译器初版交付**(2026-06-30,21 commits `a94bcec..HEAD`,80 e2e 全过):Go 端回放 + 占位 mmap stub 物理路径(设计稿 §3 native amd64 multi-BB CFG emit 是性能前提非语义前提,留作 followup);35/38 opcode 接住——PJ10a 直线(MOVE/LOADK/LOADBOOL/LOADNIL/GETUPVAL/SETUPVAL/RETURN) + PJ10b 算术/一元/CONCAT(ADD/SUB/MUL/DIV/MOD/POW/UNM/LEN/NOT/CONCAT) + PJ10c 比较 diamond + and/or diamond + FORPREP/FORLOOP + TFORLOOP + PJ10d 表/调用(GETTABLE/SETTABLE/GETGLOBAL/SETGLOBAL/NEWTABLE/SETLIST/CALL/TAILCALL/SELF/CLOSURE/CLOSE),不接 VARARG(设计永不接) + JMP/TEST/LOADBOOL C!=0(需真 CFG 留 followup);设计稿 §14 新加进度章诚实记录物理路径分歧;反思见 [[2026-06-30-p4-pj10-perop-translator-round]] / **PJ10 native emit 接上**(2026-07-01,分支 `feat/pj10-native`,`ed2235b..0c9db3a` ~17 commits):真 amd64 / arm64 原生码 emit(CFG builder + 两遍 label resolver + 35 opcode 每 arch 一份 emit)+ inline 18 op mmap-safe 子集(MOVE/LOADK/LOADBOOL/LOADNIL/ADD/SUB/MUL/DIV/NOT/EQ/LT/LE/TEST/TESTSET/JMP/FORPREP/FORLOOP/RETURN,mmap + Go morestack 物理不兼容硬约束)+ arch-aware `fixupKind` label resolver(amd64 rel32 / arm64 B26 / arm64 Cond19)+ `PreferNative` 多 BB + big-BB 收窄门(精确匹配 shape-spec 触达不到的形式)+ tail-call gibbous dispatch;**V15b heavy 三本 P4 native > P3 wasm 达标**(HeavyArith 2.3x / HeavyRecursion 2.3x / HeavyFloatloop 3.0x,10 iter × 3 samples,amd64);V14 luajc 档无回归;arm64 runtime e2e 留 CI followup(需 linux/arm64 host,QEMU 不支持 mmap RWX→RX);反思见 [[2026-07-01-p4-pj10-native-round]] / **PJ10 must-beat-P3 op 集扩面(2026-07-02,分支 `feat/p4-beat-p3`,30 commits `dc9baf0..79bd6dc`)** ✅:承 PJ10 native emit,通过 **exit-reason 协议**(`jitCtx.exitArg0` 打包 `(helperCode, a, b, c, pc)` + `ExitInlineHelper` RET + Go 端 `nativeCode.Run` dispatcher 分派 host 方法 + `RefreshJitCtxAddrs` + 从 `codePage + resumeOff` 重入)解 mmap+morestack 物理不兼容,把 opSupported 从 18 op(mmap-safe 子集)扩到覆盖 amd64 上的 CALL(密度门 `totalOps/callCount>=16`,B=0/C=0 拒)/ SETTABLE / NEWTABLE(IC ArrayHit/NodeHit gated)/ UNM(inline sign-flip + guard)/ GETUPVAL / SETUPVAL / GETGLOBAL / SETGLOBAL(NodeHit-IC gated + inline gen-check 快路径 + 18-bit Bx 拆两 9-bit payload slot)/ 多值 RETURN(HelperReturn 终结不重入);**正确性收口三 fuzz 种子驱动**:(a) `f7f0bb1a` x86 SSE arith 结果 NaN(`0xFFF8...`)与 NaN-box tag 空间别名 → arith inline 加 result guard 路由 `host.Arith` canonicalize / (b) `4b3d10ff` **`insertNewKey` Brent 式重定位改 slot 但没 BumpGen**(`internal/crescent/rawtable.go`)——解释器 `icGetTable` per-access 复验 NodeKey 掩盖多年,gen-only inline 快路径(P4 native GETGLOBAL NodeHit + P3 wasm `emitGetGlobal` 一样的)立刻 UAF;**关键 invariant**:key→slot 语义变化必须 BumpGen(producer 契约由最严 consumer 定义) / (c) `d9bce2e` LT/LE inline UCOMISD 缺 IsNumber 守卫使 reg operands boolean 时静默比较位模式;三新种子入 corpus `testdata/fuzz/FuzzP4ForceAllPromote/`;**bridge/analyzer 三改**:(i) safe stdlib alias dataflow 追踪——`funcState.localAliasAsts` 记 `local sqrt = math.sqrt` 类 AST(非 name),`AnalyzeProtoWithOuter` 加 `outerAliases` 参数,visitor 的 `safeAliases` 处理 reassignment/shadowing/scope 失效(`internal/bridge/analyzer.go` + `internal/frontend/compile/{funcstate,stmt,analyze_on}.go`)/ (ii) `MinPromotableLener` 可选接口——后端可覆盖短 proto 升层地板,`SetP3Compiler` 时快照(P4 实测 fixed Run 成本 ~111ns > 解释器 78ns tiny proto → 地板停 10)/ (iii) `considerPromotion` 加 forceAll retry window(前几次进入保持 TierInterp 让 IC-gated 后端见到 warmed IC);**性能结果**:25/27 P4-vs-P3 对基准 ≥ +2%(多在 +10%~+85%),fannkuch 5.8ms→0.6ms,nbody 升层后 P4 44.2ms 赢,2 对 CallOnly-auto 是解释器-vs-解释器噪声平手;README perf 表整节更新(f736c92 + 79bd6dc「≥1.5x 加粗」规则一致化);**已知 followup**:issue #37 arm64 exit-reason port(arm64 shim-free 设计天然适合)、issue #39 P3 nbody 回归(alias 追踪副作用,43.5→89.7ms 慢 2x;共享分析器改进可能是 per-backend 回归的实证)、task #57;反思见 [[2026-07-02-p4-beat-p3-opset-round]] / PJ11 luajc 档突破(loop 7.15-25.41x)。**arm64 exit-reason 端口已收口(2026-07-03,issue #37/#40 阶段 2,分支 `feat/p4-arm64-exit-reason` 8 commits)**:7 步移植(dispatcher 循环 + GETUPVAL/SETUPVAL → CALL 密度门 → GETGLOBAL/SETGLOBAL NodeHit inline → GETTABLE/SETTABLE ArrayHit inline + NEWTABLE → UNM(顺带修双 arch -NaN 静默变 nil 既有 bug)→ 多值 RETURN → arith/LT/LE 恢复(比较条件码换 FP 安全族 MI/PL/LS/HI)),两 arch native 接受面重新同构,darwin/arm64 M5 Pro 实测 heavy+realworld P4 全面 ≥ P3(HeavyArith 2.03× / HeavyFloatloop 2.47× over P3),详 `p4-method-jit/implementation-progress.md` §14.6 + 反思 [[2026-07-03-issue37-arm64-exit-reason-port-round]];**arm64 inline EQ 接受 K 操作数(2026-07-03,issue #56/#52,`3c4d4ce`)**:`inlineRawEqArm64` 镜像 amd64 `inlineRawEq`——K 操作数把 boxed 常量烧成 `mov imm64`(string K 槽在 LoadProgram 期 intern、升层前 bits 稳定 ⟹ ptr-equal ↔ string-equal),reg 操作数保留 ldr 路径,`AnalyzeNative` 的 EQ 严格 reg-reg 门放宽到接受任意 in-range K,补齐上轮「两 arch 接受面同构」最后一处缺口——这是 arm64 fannkuch 93-op 热 proto 的唯一静态升层阻塞项,放开后 M5 Pro 实测 force 3.62ms→0.33ms(10.9x over interp / ~7.5x over gopher-lua),auto 与 force 一致;**amd64 段到段(seg2seg)CALL 分派已交付(2026-07-06,分支 `feat/issue50-emit-call-inline`)**:段内 GETUPVAL inline + 双语义 RETURN 扩到 MultiReturn proto + arith/compare/GETUPVAL/嵌套 CALL 接受面,消掉每次 gibbous→gibbous 调用的 exit-reason 往返,call 密集内核反超 gopher-lua(fib 10.5x / fannkuch 6.9x / binary-trees 1.35x over gopher-lua,详 `p4-method-jit/implementation-progress.md` §15.7);arm64 段到段 CALL 镜像实现已写完 + 交叉编译过(真机验收经 CI 挂 issue #61);**deopt 机制现状(issue #66 裁决,2026-07-07)**:OSR 骨架经调查确认**非死代码**,区分三套 deopt——① 函数级 OSR 物化(重建解释器帧续跑,04 正文设想)从未实现,已由 #50 虚拟帧 + deopt-redo 取代(裁决 `spike/p4callinline/DECISION.md`)不再需要;② `internal/gibbous/jit/p4state.go` 的 spec-template guard-miss 记账(`p4SpecState` / `onOSRExit` / `P4Deoptimized`)在 amd64/arm64 **已激活**(`SpecP4DeoptHits` 实跑增长);③ #50 的 seg2seg deopt-redo(`segCallDeopt`)活跃,#66 已补 `SegToSegDeoptCount` 探针 + 注入测试;**P4 native opSupported 接受面扩展(2026-07-07,issue #52/#68/#69)**:三项能力面(非收益)扩展,均双 arch + prove-the-path e2e byte-equal——**#52 SELF + CONCAT 纳入 opSupported**(amd64 白名单 30→32 op,一次 `obj:method()` 或一次数值 `..` 不再拖垮整函数升层;整条 emit/dispatch/host 流水线本已存在走 exit-reason dispatch,唯一缺口是 `AnalyzeNative` 见到 SELF/CONCAT 就拒整个 proto;arm64 侧发现旧 `emitSELFArm64`/`emitCONCATArm64` 挂在 legacy shim 通道故重写走 exit-reason + 清孤儿 shim)/ **#68 SETLIST 纳入 opSupported**(binary-trees `return{left,right}` 的 NEWTABLE+SETLIST proto 得以升层,拒 B=0 fill-to-top / C=0 批量计数在下条指令字两形式,同 CALL B=0/C=0 先例;arm64 `emitSETLISTArm64` 一并从 legacy shim 重写走 exit-reason)/ **#69 字符串常量 LOADK 放开 F7-a 门**(P4 接受面最大隐性缺口——此前含活跃字符串字面量 LOADK 的 proto 被整体拒收,一个 `s="x"`/`t.name.."-"`/`obj:m("key")` 就让整函数连同算术/循环/CALL 掉回解释器;spike 方向 A 把字符串槽 GCRef 作 imm64 烤进 mmap 段,镜像 EQ-K #56,三前提 per-State 归属 / Consts 编译期已 intern / arena 非移动;emit 零改动,`emitLOADK` godoc 登记 #12 copy-compact GC 的烤死 GCRef 重定位面依赖);**三项均为接受面而非逐 op 加速**——字符串/表 op 密集 kernel 仍约等于解释器,价值是不再因一个 gap op 拖垮整函数;详 `p4-method-jit/implementation-progress.md` §16-§17;**issue #52 已收口(2026-07-09,PR #99)**:补齐最后四个 op——TAILCALL(HelperTailCall 终止 run,镜像 HelperReturn;host 臂经尾随死 RETURN B=0 multret 收尾)/ TFORLOOP(HelperTForLoop verdict 经 exitArg0 回段分支,镜像 HelperCompareSlow)/ CLOSURE(HelperClosure,upvalue 伪指令字是数据非 op,全部翻译器 pc 走查统一经共享 `nextRealPC` 步进,Bx 走 GETGLOBAL 18-bit b|c 拆分)/ CLOSE(HelperClose 普通往返),双架构同步,`opSupported` 白名单自此只剩 VARARG 一个永久设计门;**顺带修复共享三层的存量 bug**:`doTailCall` 尾调用 host 函数传父帧定长 nresults(应为 -1 multret,PUC `luaD_call(L, ra, LUA_MULTRET)`),`return unpack(t)` 丢尾部返回值 P1/P3/P4 全中,独立 commit + conformance `tailcall_host_multret` 锁三层;详 `p4-method-jit/implementation-progress.md` §21 + 反思 [[2026-07-09-issue52-close-round]];**跨 arch CI**(2026-06-29,PR #27 本批 14 commits):linux/arm64 检查翻 true ✅ + darwin/arm64 codepage 真实现(`internal/jit/jitcgo/` 子包 forward 模式保主库零 cgo)+ macos-latest M1 接入 CI 安全子集(到达 CI 绿,真 execute SIGSEGV 留 followup PR Mac 物理机调试,darwin/arm64 检查暂 false);**P3 列内核 loop 形式已超 luajc 档:loop 2.95x over P1 = 7.2x over gopher-lua > luajc 档 4.4x over gopher-lua;但 table 0.88x / call 0.52x / mixed 0.99x 仍 ≪ luajc 档列内核形式——P4 立项动机不在 loop 而在非 loop 形式**;实施现状对账 + 34 项跨文档回填请求(RJ-1~34,撤回 RJ-8/9/10,方案 A)见 `p4-method-jit/implementation-progress.md`。**P4 状态由原「架构决策深度」升级为「详细设计深度 + 多 PJ 已完成」**,与 P2/P3 同档。**PR #83(2026-07-08,fixes #78/#80/#81)修复两个 issue #67 auto-mode 覆盖轮(PR #75)`FuzzAutoPromote` 上线数天内抓到的 P4 native-tier miscompile**:FORPREP 两 arch 共有的缺 `IsNumber` 数字守卫(nil limit 静默零迭代退出而非报错,新 `HelperForPrep` exit-reason 退 `host.ForPrep`)+ 深度 native 递归破坏值栈的叠加 bug(`nativeCode.Run` 入口捕获的 `base` 在 host 调用 growStack 重定位值栈段后变悬垂 [[design-claims-vs-codebase-physics]] §2 第二实例 + seg2seg 段内直调无边界检查溢出初始栈段,新 `jitCtx.valueStackEnd` + 两 arch fast-body 守卫修复);反思见 [[2026-07-08-pr83-forprep-stackgrow-fallout-round]]。**amd64 GETTABLE/SETTABLE NodeHit 跨 Run inline 补齐(2026-07-08,issue #67,PR #82)**:把 arm64 上已修正的 identity-free NodeHit inline(hmask 边界 `Index <= hmask` + `nodeRef != 0` guard,替换烤进段的 TableRef 身份 guard)移植到 amd64,共用 `emitTableNodeHitPrelude`,推翻 PR #74「amd64 NodeHit ~3% 回归故 arm64-only」历史裁决(那 ~3% 是身份 guard 跨 Run 100% 落空的白费开销、inline 从没真生效);实测 amd64 n-body auto 43.5ms→7.0ms(~6.2×),跨 Run exit-reason dispatch 875k→50k,fib/binary-trees/spectral-norm/fannkuch 无回归,新增 `TestPJ10_TableNodeHit_CrossRunInline` 断言每 Run dispatch<500;反思见 [[2026-07-08-issue67-amd64-nodehit-crossrun-round]]。**math.* intrinsic emission(2026-07-08,issue #77,PR #87)**:CALL 站点 IC 观察到被调是已知纯数值 host closure(sqrt/floor/ceil/abs/max/min)时段内直接发射硬件指令(amd64 SQRTSD/ROUNDSD/sign-clear/UCOMISD-select;arm64 FSQRT/FRINTM/FRINTP/FABS/FCMP+FCSEL),不再 exit-reason 往返 Go host closure;intrinsic 检查挂在 host closure 必走的 protoID guard-miss 慢路径故热 Lua 调用零开销,byte-equal 依据在 `value.NumberValue` 的 canonNaN 规范化(result-NaN guard 抓走负 indefinite 路由 host);是 #67 NodeHit inline 后 n-body 剩余 dispatch(几乎全是 sqrt)的直接后续;反思见 [[2026-07-08-issue77-math-intrinsics-round]]。**PR #86(fixes #85,files #89)深递归双 bug 修复**:#85 深升层递归误报假 "C stack overflow"(P4 路径超 seg2seg 深度上限后每层递归是真 Go 重入链撞 `nCcalls` 上限 200),修法 `gibbousReentryCCallCap = maxCCallDepth/2` 软水位线、四个 gibbous 派发入口超线回落解释器平坦执行,深度约束回到 20000 与 P1 逐字节一致;#89(存量 bug,被 #85 修复暴露)seg2seg 段内直调 `sub sp` 在 morestack 不可触发的 NOSPLIT 窗口穿透栈守卫踩坏相邻堆对象,`segToSegDepthCap` 128→16(worst case 608B<`StackNosplitBase` 800B),提回需接 05 §3.4 自管 spill 栈;反思见 [[2026-07-08-pr86-deep-recursion-nosplit-round]]。

**P3 PW0-PW10 全卷已收口**(spike 检查过线 → 包骨架 → 直线 opcode + trampoline + VS0 → 算术/比较 → 控制流 relooper + FORPREP/FORLOOP → TFORLOOP 泛型 for → 表 IC inline 固化跳哈希 → CALL/TAILCALL 跨层三向互调(base 刷新解 growStack 重定位 UAF)→ CLOSURE/CLOSE → 线程级 tier 规则(协程不升层)→ PW9 端到端总验收(正确性轴 V1-V13 三方逐字节 + V17 四 build + V18 -race + 性能轴 V14 loop 核 2.58x≥2x 全过));全 38 opcode 除 VARARG 外全部可翻译,**PW10 消除 gibbous→gibbous 跨层调用税:R1/R2/R3/R3.5 + 零跨界 ①/基建-a/基建-b/③a/③b/④-i 骨架/顶层升层 callOnStack 全过线**(Phase 0 spike `spike/p3indirect/` 裁定 Arch-2 共享 imported funcref 表而非 rebuild-all,决策报告 `spike/p3indirect/DECISION.md` + R1 共享 funcref 表基建 + R2 CallInfo→linear-memory 迁移 = 延后的 VS0-e,4 word/帧 + R3 `call_indirect` 直接分派消 `code.Run` 重入 + R3c-fix + R3.5 host helper 零分配 + 零跨界 ① top mirror 字基建 + 基建-a closure slot 缓存 + 基建-b proto cache 段 + ③a savedTop + ③b emitReturn 守卫快路径 + ④-i emitCall 守卫骨架 + fastCallHits mirror 字 + callOnStack 顶层升层 + TopLevelUplift 探针 + emit 原语 i64.add/i64.or 保留供未来 ④-ii);**本机 Xeon 6982P 2s×3 count 实测基线(2026-06-16)**:**loop 2.95x(+10% over R3.5 2.67x,③ RETURN 拆帧真实收益)/ table 0.88x / call 0.52x / mixed 0.99x**;**call 0.52x 是 bench kernel 结构性架构边界**(profile `/tmp/call.prof` 证四 kernel body 含 ReasonUnknownCall F2-b 不可升 ⟹ 顶层升层 + ④ emitCall fast body 均对 bench kernel 无显著效果);**④-ii fast body 未交付**(预估上限 0.57x 仍 <1x,ROI/UAF 不利留 followup);**PW10 收口为「已完成子里程碑 + 架构边界文档化」**,旧文档所述「四核全翻面」+「剩 R4/R5 待实现」失实须替换;实施现状对账(PW0-PW9 ✅ + §12 PW10 R1-R2 + §13 R3+R3.5 + §14 零跨界 RETURN 拆帧 + §14.8 ④-i + §14.9 顶层升层 + §14.10 ④-ii 架构边界 + §7-§11 实现 vs 设计对账表)+ 跨文档回填请求收口表(RW-1~11)见 `p3-wasm-tier/implementation-progress.md`。
- **工程化机制**:`docs/design/engineering.md`——Git hooks(三件套含 commit-msg 强制 `type(scope):`)/CI workflows(ci + nightly-diff-fuzz 自动开 issue + nightly-benchmark)/Makefile 唯一任务入口/`-race` 硬门禁/oracle 供给(lua5.1=5.1.5 校验 fail-fast)/M0 工程地基里程碑。与 12 的分工:12 管「测什么」,engineering 管「机制怎么搭」。其中 nightly-diff-fuzz 已完成(`.github/workflows/nightly-diff-fuzz.yml`,triage 内联非独立脚本,差异对账见 doc-gaps);nightly-benchmark 未建。
- llmdoc 不搬运设计文档内容,只做压缩与路由;深入实现细节一律回源文档。
